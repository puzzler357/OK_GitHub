import path from 'node:path';

/**
 * Отдельная база для теста сброса системы.
 *
 * Тест чистит все таблицы и восстановить их не может: посев выполняется один
 * раз за жизнь файла БД. Порядок файлов vitest не гарантирует — по умолчанию
 * он сортирует их по размеру, а не по имени, — поэтому такой тест, запущенный
 * раньше остальных, вытирал бы базу под ними.
 *
 * Модуль импортируется первой строкой reset.test.ts: значение должно попасть
 * в окружение до загрузки server.ts, который тянет src/db/sqlite.ts, читающий
 * DB_PATH на этапе импорта.
 */
export const RESET_DB_PATH = path.join(__dirname, '..', '.tmp', 'api-reset-test.db');

process.env.DB_PATH = RESET_DB_PATH;
