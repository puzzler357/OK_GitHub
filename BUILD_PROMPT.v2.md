# HRDesk — актуальный мастер-промпт (v2)

> **Статус документа:** заменяет `BUILD_PROMPT.md` (v1) и `TZ.md`.
> v1 описывал проект «с нуля». Этот документ описывает **фактически существующий код** и только те работы, которые реально остались.
>
> **Правило приоритета:** код в `src/` — источник истины. Если этот документ расходится с кодом, прав код, а документ надо поправить.
>
> **Дата среза:** соответствует состоянию репозитория `d:\ОК` на 2026-08-01.

---

## Зафиксированные решения заказчика

Приняты 2026-08-01. Отменяют соответствующие пункты `TZ.md` v1.0 и закрывают спорные места аудита.

| Решение | Что это значит | Последствия |
|---|---|---|
| **Платформа: только Windows** | Целевая ОС — Windows 10/11 x64. Сборка `nsis` + `msi`, как сейчас в `tauri.conf.json` | Требования по macOS (`.dmg`) и Linux (`.AppImage`/`.deb`) удаляются. Кроссплатформенное тестирование из плана убирается |
| **Валюта настраиваемая** | Символ, позиция (префикс/суффикс), число знаков после запятой и разделитель разрядов задаются в настройках | Задача 4.0.4 переписана. Текущий разнобой `₽`/`₸` устраняется |
| **Только десктоп** | Мобильной и планшетной вёрстки не будет. Минимальное окно — 1024×700 (уже в `tauri.conf.json`), опорное разрешение 1280×800 | Задача 4.4.7 заменена: вместо адаптивности — корректность в узком окне |
| **Порог 10 000 сотрудников** | Требование производительности из ТЗ подтверждено: список из 10 000 сотрудников должен прокручиваться плавно, основные экраны открываться ≤ 1 с | **Меняет архитектуру хранения — см. раздел 4.П.** Виртуализация возвращается в обязательный скоуп |

| **Пять макетных модулей — довести до БД** | `Recruiting`, `TimeOff`, `Onboarding`, `Performance`, `KnowledgeBase` перестают быть демо: получают таблицы в SQLite и полноценный CRUD | Задачи 4.1.1–4.1.5 подтверждены как обязательные. Эти модули входят в критерии приёмки и в требование «все данные в SQLite» |

Открытых вопросов нет. Скоуп зафиксирован полностью.

---

## 0. Роль и незыблемые правила

Ты — senior full-stack инженер, дорабатывающий **существующее** десктоп-приложение кадрового учёта HRDesk. Приложение локальное, однопользовательское, офлайн: один владелец устройства, одна локальная SQLite, никакой сети, синхронизации, ролей и мультипользовательского режима.

**Главное правило: ничего не переписывать «с нуля».** Каждая задача ниже — точечная доработка поверх работающего кода. Прежде чем менять файл — прочитай его целиком.

**Что менять запрещено без явного запроса пользователя:**
- Систему дизайн-токенов в `src/index.css` (8 уровней фона, 3 границы, 4 текста, 6 акцентов). Она уже полностью покрывает светлую и тёмную темы — использовать её, а не хардкодить `slate-*`/hex.
- Фасад доступа к данным `src/data/index.ts` и его двухрежимную схему (Tauri / web-dev).
- Структуру трёх файлов локализации (`src/locales/{ru,en,tk}.ts`) — только дополнять, сохраняя паритет ключей.
- Стек: React 19, Vite 6, Tailwind v4 (CSS-first), Zustand, React Router 7. Никакого shadcn/ui и Radix — в проекте их нет и вводить их не нужно.

**Обязательные правила разработки:**
1. Все пользовательские строки — через `t('...')`. Хардкод русского текста в JSX — дефект.
2. Новые ключи локализации добавлять сразу в три файла (ru/en/tk), паритет ключей обязателен.
3. Новые сущности — сначала таблица SQLite **в обоих** слоях доступа (`src/db/sqlite.ts` и `src/data/tauriDb.ts`), затем метод в `src/data/index.ts`, затем действие в `useDatabaseStore`.
4. Цвета — только токены (`bg-surface-*`, `text-primary/secondary/muted/faint`, `border-line/subtle/strong`, `accent-*`).
5. `tabular-nums` для чисел, `rounded-xl`/`rounded-2xl` для карточек — как в существующих экранах.
6. После каждой задачи: **`npm run verify`** должен проходить целиком. Это цепочка `lint` → `build` → `test` (Vitest, API) → `test:e2e` (Playwright, обход всех экранов). Отдельные шаги — `npm run lint`, `npm run build:client`, `npm test`, `npm run test:e2e`.
7. Добавил маршрут в `src/App.tsx` — сразу добавь его в `tests/e2e/routes.ts`, иначе экран выпадет из автоматического обхода. Добавил или изменил эндпоинт в `server.ts` — покрой его в `tests/api/crud.test.ts`.
8. Красный тест — это дефект в коде, а не в тесте. Ослаблять проверку (расширять список игнорируемых ошибок консоли в `tests/e2e/pageWatcher.ts`, удалять ассерт) допустимо только для доказанного шума инфраструктуры и с комментарием, объясняющим почему.

---

## 1. Фактическая архитектура (as-is, не переделывать)

### 1.1. Стек по факту

| Слой | Что стоит | Примечание |
|---|---|---|
| UI | React **19.0.1** + TypeScript 5.8 + Vite 6 | не React 18, как в v1 |
| Стили | **Tailwind CSS v4** через `@tailwindcss/vite`, конфиг CSS-first в `src/index.css` (`@theme`) | `tailwind.config.js` отсутствует и не нужен |
| Компоненты | собственная разметка на Tailwind | shadcn/ui и Radix **не используются** |
| Десктоп | Tauri 2 (`src-tauri/`), продукт `HRDesk`, id `com.hrdesk.local` | сборка только `nsis` + `msi` (Windows) |
| Состояние | Zustand + `persist` (2 стора) | TanStack Query установлен и провайдер смонтирован в `main.tsx`, но **ни одного `useQuery` в коде нет** |
| Роутинг | React Router 7, `BrowserRouter` | 18 маршрутов |
| Формы | React Hook Form + Zod | используются **только** в `EmployeeForm` |
| Таблицы | TanStack Table | используется **только** в `Employees`; виртуализации нет |
| Графики | Recharts | только `Dashboard` |
| БД | SQLite | `local-hr-docs.db` |
| i18n | i18next + react-i18next, локали как **TS-модули**, не JSON | ~440 ключей, паритет ru = en = tk |
| Экспорт | ExcelJS (`lib/excel.ts`), SheetJS (импорт), html2pdf (`Archive`), `window.print()` | |
| DnD | `@hello-pangea/dnd` | только `Recruiting` |
| Rich text | TipTap 3 (StarterKit + TextStyle + Color) | только `KnowledgeBase` |
| Тесты API | **Vitest 3** (`tests/api/`) | 19 тестов: авторизация и CRUD всех сущностей |
| Тесты UI | **Playwright** (`tests/e2e/`), проект `chromium` | 23 теста: вход и обход всех 18 экранов |

### 1.2. Двухрежимный доступ к данным (ключевое архитектурное решение проекта)

`src/data/index.ts` — единый фасад. Ветвление по `isTauri = '__TAURI_INTERNALS__' in window`:

- **Tauri (продакшн):** `src/data/tauriDb.ts` — прямые SQL-запросы через `@tauri-apps/plugin-sql`, схема и сид создаются на первом `getDb()`. Сети нет вообще. Аутентификация локальная через bcryptjs, токен-заглушка `'local'`.
- **Web-dev (`npm run dev`):** `server.ts` — Express + `better-sqlite3` + Vite middleware, REST на `/api/*`, схема и сид в `src/db/sqlite.ts`, JWT для логина.

Оба слоя дают одинаковый набор функций: `listEmployees/createEmployee/updateEmployee/deleteEmployee`, то же для `departments`, `positions`, `templates`, `timesheets`, `archives` (у архивов нет update), плюс `login`, `changePassword`, `resetSystem`.

> **Осознанный долг:** схема БД описана дважды (`src/db/sqlite.ts` и `src/data/tauriDb.ts`) и уже разошлась — в `sqlite.ts` есть `ALTER TABLE` для `birth_date`/`payment_type`/`salary`/`rate`, в `tauriDb.ts` эти колонки сразу в `CREATE TABLE`, миграций нет. См. задачу 4.1.

### 1.3. Сторы

**`useAppStore`** (`persist: 'hr-docs-app-storage'`) — UI и сессия:
`theme` (light/dark/system, дефолт **dark**), `accentColor` (indigo/purple/blue/emerald/rose/amber), `density` (compact/standard/spacious), `fontSize` (small/standard/large), `language`, `sidebarOpen`, `user`, `token`.
Настройки применяются как классы на `<html>` (`applyTheme/applyAccentColor/applyDensity/applyFontSize`) и дополнительно восстанавливаются синхронным IIFE до гидратации — чтобы не было вспышки темы.

**`useDatabaseStore`** (`persist: 'hr-docs-local-db'`) — сущности: `employees, departments, positions, timesheets, archives, templates`. `fetchAll()` дёргается из `App.tsx` после логина. Мутации оптимистичные: сначала `set`, затем запрос.

### 1.4. Схема БД — 7 таблиц

`users(id, email, password_hash, role, name)` · `employees(id, full_name, position, department, status, hire_date, birth_date, payment_type, salary, rate)` · `departments(id, name, parent_id)` · `positions(id, department_id, title, max_count, salary)` · `templates(id, name, blocks JSON)` · `timesheets(id, year, month, employee_id, days JSON)` · `archives(id, year, month, employee_id, employee_name, department, position, salary, hours_worked)`

**Таблиц нет** для: кандидатов, заявок на отсутствия, чек-листов адаптации, целей/ревью, статей базы знаний, движений кадров, журнала аудита, бэкапов, настроек организации.

### 1.5. Дизайн-система (готова, использовать как есть)

`src/index.css`:
- Фоны: `app → section → surface → surface-2 → surface-3 → surface-4 → input → surface-hover`
- Границы: `line / subtle / strong`; Текст: `primary / secondary / muted / faint`
- 6 акцентных палитр как классы `.theme-*` на `<html>`, каждая маппит `--accent-50..900`
- Плотность управляет `--table-p` / `--table-text`; размер шрифта — `--app-font-size` на `html`
- Утилиты-компоненты `.p-table` / `.text-table`; Recharts перекрашен токенами через CSS; блок `@media print` с `.no-print` / `.print-container` / `.print-only`
- Шрифт Inter подключается `@import url(fonts.googleapis.com)` — **внешний запрос, в офлайн-сборке не резолвится** (см. задачу 4.4)

### 1.6. Навигация

Сайдбар (`Layout.tsx`) — **14 пунктов**: Главная, Сотрудники, Штатное расписание, Рекрутинг, Отсутствия, Табель, Адаптация, Оценка, База знаний, Шаблоны справок, Генератор документов, Отчёты, Архив, Настройки.

Маршруты **без пункта меню**: `/movements`, `/calendar`, `/profile`, `/templates/new`. Первые два недостижимы из UI вообще.

Топбар содержит только бургер и заголовок страницы. Глобального поиска, переключателя языка/темы, уведомлений и меню пользователя в топбаре **нет** (в v1 они требовались; сейчас язык и тема живут в Настройках, тема ещё и на экране логина).

### 1.7. Тестовый контур (введён 2026-08-23, не ломать)

Два независимых набора, обе точки входа собраны в `npm run verify`.

| Набор | Где | Чем гоняется | Что проверяет |
|---|---|---|---|
| API | `tests/api/` | Vitest 3 | `auth.test.ts` — вход, неверный пароль, несуществующий пользователь, наличие JWT, отсутствие `password_hash` в ответе, смена пароля с откатом. `crud.test.ts` — чтение всех шести коллекций, camelCase-маппинг, разбор JSON-поля `blocks`, полные циклы create→read→update→delete по сотрудникам, подразделениям, должностям, шаблонам, табелю, архиву |
| UI | `tests/e2e/` | Playwright, проект `chromium` | `auth.setup.ts` — вход один раз, `storageState` переиспользуется. `pages.spec.ts` — редирект с `/`, обход всех 18 экранов, прокликивание всех ссылок сайдбара. `login.spec.ts` — без сессии показывается форма входа, неверный пароль не пускает внутрь |

**Два контракта, на которых всё держится — их нельзя ломать:**

1. **`DB_PATH`** (`src/db/sqlite.ts`) — путь к файлу БД. Не задан → рабочая `local-hr-docs.db`. Оба набора тестов поднимают собственную временную базу в `tests/.tmp/`, поэтому рабочие данные не трогаются никогда.
2. **`HR_NO_AUTOSTART=1`** (`server.ts`) — импорт модуля без запуска слушателя. `server.ts` экспортирует `app` и `startServer`; API-тесты поднимают Express прямо в своём процессе через `app.listen(0)`, без vite-мидлвари. Отсюда весь набор укладывается в ~3 секунды.

Прочие детали, о которые легко споткнуться:

- `DISABLE_HMR=true` выставляется для e2e из `playwright.config.ts`. Без этого HMR-вебсокет vite сыплет ошибками в консоль и мешает дождаться `networkidle` — первый прогон уронил из-за него все 18 страниц.
- `tests/e2e/pageWatcher.ts` считает страницу сломанной при любой ошибке консоли, необработанном исключении или ответе ≥400. Список игнорируемого шума — там же, в одном месте.
- Список маршрутов вынесен в `tests/e2e/routes.ts` и должен совпадать с роутером в `src/App.tsx`.
- Первый запуск e2e на новой машине требует `npx playwright install chromium` (~250 МБ, один раз).
- HTML-отчёт последнего прогона: `tests/.tmp/playwright-report/index.html`. Каталог `tests/.tmp/` в `.gitignore`.

**Граница покрытия.** Тесты гоняют веб-режим через Express. Ветка Tauri (`src/data/tauriDb.ts`) автотестами **не покрыта** — при правке слоя данных её по-прежнему нужно проверять руками через `npm run tauri:dev`. Проверяется, что экраны открываются и API отвечает, а не что расчёты верны — расчётная логика ждёт покрытия в задаче 4.4.8.

**Ручной сценарий — `TEST_SCENARIO.md`.** То, чего автотесты не видят: правильность расчётов, сохранность данных при перезагрузке, читаемость обеих тем, здравый смысл цифр. 148 шагов на данных вымышленной организации (12 сотрудников, 5 подразделений, 17 штатных единиц), контрольные суммы посчитаны заранее и сверены по коду. Прогонять перед любой демонстрацией заказчику и после задач Приоритета 0. Меняешь расчётную логику или сид — синхронизируй раздел «Контрольные суммы», иначе сценарий начнёт врать.

---

## 2. Реестр экранов: что реально работает

Легенда: **[Р]** работает на реальных данных · **[Ч]** частично (UI есть, часть данных/кнопок фиктивна) · **[М]** макет на локальном стейте, в БД не сохраняется · **[З]** заглушка

| Экран | Файл | Статус | Что есть по факту |
|---|---|---|---|
| Login | `pages/Login.tsx` | **Р** | Логин работает. Поля предзаполнены `admin@global.tech / password123`, подсказка с паролем выведена на экран. Строки «Пароль», «Войти», подзаголовок — **хардкод по-русски**. Переключатель темы работает. |
| Dashboard | `pages/Dashboard.tsx` | **Ч** | Реально: число сотрудников, число шаблонов, ФОТ (`salary × rate`, при пустом окладе подставляется 85000), дни рождения сегодня, распределение по отделам (Recharts). Фиктивно: плитка «Вакансии» (формула-заглушка `max(len+4,10) − len`), лента «Последние события» — 2 захардкоженные записи. |
| Employees | `pages/Employees.tsx` | **Ч** | TanStack Table, сортировка, глобальный поиск, экспорт в Excel, дровер создания/редактирования (RHF + Zod, локализованные сообщения), удаление с подтверждением, генерация справки из карточки. Фиктивно: колонка «Таб. номер» = `Math.random()` (меняется на каждом рендере), три селекта-фильтра (отдел/статус/ставка) ни к чему не подключены, кнопка импорта Excel скрыта классом `hidden` (сам `parseExcel` работает). Валюта — `₽`. |
| OrgChart | `pages/OrgChart.tsx` | **Ч** | Реально: список подразделений, CRUD подразделений и должностей через модалки, подсчёт занято/вакантно/всего по `positions.max_count` vs фактическим сотрудникам. Фиктивно: дерево плоское (фильтр `d.parentId === 'd1' \|\| d.parentId !== null` пропускает почти всё, вложенности нет), кнопка «Новая редакция» ничего не делает, кнопки edit/delete должности в таблице — пустой контейнер. Валюта — `₸`. |
| Movements | `pages/Movements.tsx` | **З** | Только вёрстка: три кнопки, фильтры, таблица с текстом «Загрузка…». Данных нет, таблицы `movements` в БД нет, пункта меню нет. |
| Templates | `pages/Templates.tsx` | **Р** | Сетка шаблонов из БД, поиск, переход в конструктор, удаление. |
| TemplateBuilder | `pages/TemplateBuilder.tsx` | **Ч** | Блоки text/table/signature, добавление и удаление, редактирование имени, сохранение в БД, печать, палитра `{{переменных}}` с копированием. Нет: drag-and-drop перестановки блоков, условной логики, версионирования, настройки таблицы (блок `table` рендерится фиксированной заглушкой). |
| DocumentGenerator | `pages/DocumentGenerator.tsx` | **Р** | Выбор шаблона и сотрудника, автоподстановка `fullName/position/department` (+ русские алиасы `ФИО/Должность/Отдел`), ручной ввод остальных переменных, предпросмотр «на бумаге», печать в PDF через `window.print()`. |
| Reports | `pages/Reports.tsx` | **Ч** | 5 преднастроенных отчётов. На реальных данных: «Списочный состав» (1) и «Штатная расстановка» (5, частично — колонка «Штатных единиц» = `count+1`, «Вакантно» = константа 1). Мок: «Текучесть» (2) — 4.2% / 12, «Табель» (3) — сгенерированные 15 дней, «Движения» (4) — фейковые приказы. Кнопка «Скачать Excel» не подключена. Вкладка «Конструктор (beta)» — плашка-заглушка. |
| Timesheet | `pages/Timesheet.tsx` | **Р** | 10 буквенных кодов Т-13, навигация по месяцам, фильтр по подразделению, клик по ячейке циклически меняет код, выходные подсвечиваются и подставляются по умолчанию, итоги (отработано дней / явок / часов), sticky-колонки слева и справа, сохранение в БД. Кнопка «Скачать» не подключена. |
| CalendarView | `pages/CalendarView.tsx` | **З** | Полностью статичная сетка: захардкожен «июль 2026», 31 день, ручные пустые ячейки, панель «Ближайшие события» показывает «Загрузка…». Стрелки не работают. Пункта меню нет. |
| Recruiting | `pages/Recruiting.tsx` | **М** | Канбан с рабочим drag-and-drop, поиск, фильтр по должности, модалка добавления кандидата. Всё в `useState`, при перезагрузке теряется. Плитки 12/48/8 — константы. Колонок 4, а статусов в типе 6 (`hired`/`rejected` некуда перетащить — карточка исчезнет из UI). **Баг:** в двух `className` использован `\${...}` вместо `${...}` (строки 275, 284) — подсветка зоны дропа и стиль перетаскиваемой карточки не применяются. |
| TimeOff | `pages/TimeOff.tsx` | **М** | Вкладки «Мои» / «Команда», создание заявки, approve/reject. Локальный стейт, 3 демо-заявки. Плитки «24 дня» и «1 заявка» — константы. Даты вводятся строкой, не датой. Разметка обещает 3 колонки, карточек — 2. |
| Onboarding | `pages/Onboarding.tsx` | **М** | Чек-листы onboarding/offboarding, отметка задач, модалка деталей с полем комментария (не сохраняется). Локальный стейт, задачи и ФИО захардкожены по-русски. Счётчики «активных» — константы. |
| Performance | `pages/Performance.tsx` | **М** | Цели с прогресс-барами, добавление цели, модалки запуска ревью и заполнения 360°. Локальный стейт, подтверждение через `alert()`. История развития и блок «Outstanding» — статика. |
| KnowledgeBase | `pages/KnowledgeBase.tsx` | **М** | Полноценный TipTap-редактор (bold/italic/strike/H1/H2/списки/2 цвета), создание статьи, просмотр, поиск по заголовку и содержимому, фильтр по категории. Локальный стейт; 4 категории и 4 статьи захардкожены по-русски, счётчики категорий не связаны с реальным числом статей. |
| Archive | `pages/Archive.tsx` | **Р** | Данные из таблицы `archives`, фильтры год / подразделение / сотрудник, экспорт в Excel (SheetJS), PDF (html2pdf), печать. Валюта — `₸`. Записи в архив из UI никогда не создаются — только сид. |
| Settings | `pages/Settings.tsx` | **Ч** | 7 вкладок. Работают: **Оформление** (тема, 6 акцентов, 3 плотности, 3 размера шрифта — всё применяется мгновенно и персистится), **Пароль** (реальная смена через `api.changePassword`), **Безопасность → сброс системы** (реальный `api.resetSystem` с проверкой пароля + очистка localStorage). Не работают: «Общие» (поля организации/ИНН/директора не привязаны к стейту, «Сохранить» — пустышка), «Лицензия» (пустая), журнал аудита (`mockAudit`, 3 записи), «Резервное копирование» (`mockBackups`, все кнопки-пустышки), «Импорт/экспорт» (мастер из 4 шагов — только вёрстка). |
| Profile | `pages/Profile.tsx` | **М** | Шапка с реальными `user.name/email`, 5 вкладок (обзор, расчётные листки, отсутствия, техника, задачи) — все на захардкоженных русских данных. Кнопки «Редактировать профиль» и «Изменить пароль» ничего не делают. Смены языка/темы/фото на этом экране нет (в v1 требовались). |

---

## 3. Расхождения со старым промптом (v1)

### 3.1. Уже реализовано — **не делать повторно**

- Дизайн-система: полноценные светлая и тёмная темы на CSS-переменных, `system`-режим, 8 уровней фона, семантические границы и текст, Inter, `rounded-xl`, `tabular-nums`, печатные стили. Аудит от 2026-07-22 закрыл 1028 цветовых дефектов.
- Трёхъязычность (tk/en/ru), ~440 ключей с подтверждённым паритетом, единый бренд `HRDesk`, локализованные `alert`/`confirm`.
- Локальный вход единственного владельца, bcrypt-хеш, смена пароля, сброс системы с проверкой пароля.
- Tauri 2 + локальная SQLite без сети; сборка Windows-инсталляторов.
- Карточка сотрудника на RHF + Zod, список на TanStack Table, экспорт Excel через ExcelJS, импорт через SheetJS.
- Оргструктура: CRUD подразделений и должностей, штатные единицы, счётчики занято/вакантно.
- Конструктор шаблонов + генератор документов с подстановкой переменных и предпросмотром.
- Табель Т-13 с 10 кодами и сохранением в БД.
- Настройки: тема, акцент, плотность, размер шрифта, язык, смена пароля, сброс системы.

### 3.2. Реализовано **лучше**, чем требовал v1 — сохранить

| Что | v1 требовал | Что в проекте |
|---|---|---|
| Темы | «светлая и тёмная через CSS-переменные» | + 6 акцентных палитр, + 3 уровня плотности, + 3 размера шрифта, + 8-уровневая шкала фонов, + защита от вспышки темы при загрузке |
| Модули | кадры, ШР, справки, отчёты, обмен, настройки | + Рекрутинг (канбан с DnD), + Отсутствия, + Адаптация/Offboarding, + Оценка 360°, + База знаний с TipTap, + Архив с PDF/печатью, + Профиль |
| Табель | не был отдельным требованием | полноценный Т-13 со sticky-колонками, циклическим вводом, автовыходными и итогами |
| Локализация | «строки в json-файлах» | TS-модули (типобезопасно, попадают в бандл, работают офлайн), паритет проверен программно |
| Экспорт архива | Excel + JSON | Excel + PDF + прямая печать |
| Доступ к данным | «локальный бэкенд» | двухрежимный фасад: реальный офлайн-SQLite в Tauri **и** Express для быстрой веб-разработки, один API для UI |

### 3.3. Устарело — из требований **исключено**

| Требование v1 | Почему снято |
|---|---|
| React 18 | стоит React 19 |
| shadcn/ui (Radix) | не используется; вся разметка собственная. Внедрение сейчас = переписать 18 экранов ради нулевой пользы |
| `packages/shared`, монорепо `apps/client` | проект плоский: `src/` + `server.ts` + `src-tauri/`. Монорепо для однопользовательского приложения избыточно |
| JSON-файлы локализации `tk.json/en.json/ru.json` | локали — TS-модули, это лучше; переезд на JSON — регресс |
| «Единые Zod-схемы для клиента и сервера» | сервер существует только как dev-обвязка; в продакшн-сборке (Tauri) его нет. Валидация нужна на клиенте |
| Сборка под macOS (`.dmg`) и Linux (`.AppImage`/`.deb`) | **Решением заказчика платформа — только Windows.** `tauri.conf.json` уже таргетит `nsis` + `msi`, менять нечего |
| Мобильная и планшетная вёрстка | **Решением заказчика — desktop-only.** Минимальное окно 1024×700 задано в `tauri.conf.json` |
| Tauri updater, подпись сборок | автообновление противоречит «100% офлайн, без сети» |
| Живой обмен с 1С по OData/REST | и в v1 было запрещено; повторять запрет не нужно |
| E-mail / push-уведомления | нет и не будет — приложение офлайн |
| ~~Playwright e2e~~ | **Строка отменена 2026-08-23.** Требование восстановлено и выполнено: контур описан в разделе 1.7, обоснование пересмотра — `TZ_AUDIT.md`, раздел «Пересмотр от 2026-08-23». Юнит-тесты расчётной логики (4.4.8) остаются отдельной задачей, а не заменой e2e |
| «Роли и права доступа к шаблонам» | приложение однопользовательское, единственная роль `ADMIN`; всё доступно владельцу |
| Область действия настроек / настройки «по ролям» | один пользователь — областей нет |

### 3.4. Отсутствует — **это и есть остаток работ**

Полностью нет: движения кадров и история кадровых событий (аудит по сотруднику) · версионирование ШР и шаблонов · конструктор отчётов · реальный журнал аудита · резервное копирование и восстановление · многоуровневый сброс данных · мастер импорта с маппингом полей · экспорт в JSON, XML, CSV · генерация `.docx` в UI (модуль `lib/docx.ts` написан, но ни одна страница его не импортирует) · блокировка по бездействию · шифрование БД · форматирование дат и чисел по локали · **виртуализация списков и готовность к 10 000 сотрудников** · тосты вместо `alert()` · скелетоны и состояния ошибки · тесты · ESLint/Prettier · `strict` в TypeScript.

Мобильная вёрстка в этот список **не входит**: решением заказчика приложение desktop-only, её отсутствие дефектом не считается.

---

## 4. Задачи: что делать дальше

Порядок = приоритет. Каждая задача завершается зелёными `npm run lint` и `npm run build:client`.

### Приоритет 0 — дефекты в существующем коде

**4.0.1. Починить интерполяцию в Recruiting.**
`pages/Recruiting.tsx:275` и `:284` — заменить `\${snapshot...}` на `${snapshot...}`. Сейчас классы подсветки drop-зоны и перетаскиваемой карточки не применяются.

**4.0.2. Убрать `Math.random()` из рендера.**
`pages/Employees.tsx` — колонка `tabNumber` генерирует новое число на каждом рендере. Либо добавить колонку `tab_number` в `employees` (обе схемы + фасад + форма), либо убрать колонку. Рекомендуется первое: табельный номер — реальный кадровый реквизит.

**4.0.3. Подключить фильтры на экране «Сотрудники».**
Три селекта (подразделение / статус / ставка) и кнопка «Сбросить» — декорации. Подключить к `columnFilters` TanStack Table; опции подразделений брать из `departments`, а не из строк сотрудников.

**4.0.4. Настраиваемая валюта.**
Сейчас в коде разнобой: `₽` в `Employees`, `₸` в `OrgChart` и `Archive`, а на `Dashboard` сумма делится на миллион и подписывается «M» без единицы вообще.

Ввести в «Настройки → Общие» группу параметров и хранить их в `useAppStore` (персистится автоматически):
- `currencySymbol` — строка, по умолчанию `TMT` (манат);
- `currencyPosition` — `prefix` | `suffix`, по умолчанию `suffix`;
- `currencyDecimals` — 0 | 2, по умолчанию 0;
- `thousandsSeparator` — пробел / запятая / точка / нет, по умолчанию неразрывный пробел.

Добавить в `src/lib/utils.ts` функцию `formatMoney(value: number): string`, читающую эти параметры, и применить её **везде, где выводится сумма**: `Employees` (колонка «Оклад»), `OrgChart` (колонка «Оклад» и модалка должности), `Archive` (колонка «Оклад»), `Dashboard` (плитка ФОТ), `Reports` (отчёт по штатной расстановке), `DocumentGenerator` (подстановка `{{salary}}`), а также экспорты в Excel и PDF. Хардкод `₽`/`₸`/`.toLocaleString('ru-RU')` вычистить полностью.

**4.0.5. Расширить `resetSystem`.**
Сейчас удаляет только `employees` и `templates` — в БД остаются `departments`, `positions`, `timesheets`, `archives`. Поправить в `src/data/tauriDb.ts` и `server.ts` одинаково. Пересекается с задачей 4.7.

**4.0.6. Исправить дерево оргструктуры.**
`pages/OrgChart.tsx` — условие `d.parentId === 'd1' || d.parentId !== null` пропускает любой узел с непустым родителем, вложенность не рендерится. Сделать рекурсивный рендер дерева по `parentId` с раскрытием/сворачиванием узлов. Корень (`parentId === null`) показывать как «Компания».

**4.0.7. Убрать чтение стора через `getState()` в рендере.**
`components/Layout.tsx:109,113` — `useAppStore.getState().user` не подписывает компонент на изменения. Использовать уже полученный из хука `user`.

**4.0.8. Починить импорт Excel — он не сохраняет данные.**
`pages/Employees.tsx` → `handleImport` кладёт разобранные строки через `setEmployees([...data, ...mapped])`, а `setEmployees` в `useDatabaseStore` делает только `set({ employees })` — **вызова API нет, в SQLite ничего не попадает**. Импортированные сотрудники живут в localStorage до первого `fetchAll()`, после чего исчезают без следа.

Переписать на массовую вставку: новый метод `createEmployeesBulk(rows)` в обоих слоях доступа (в Tauri — одной транзакцией), действие в сторе, затем обновление списка. Заодно снять с кнопки импорта класс `hidden` и вынести захардкоженный маппинг (`row['ФИО'] || row['fullName']`) в мастер импорта — см. 4.3.5.

### Приоритет 0.5 — масштаб 10 000 сотрудников (блокирующее)

Заказчик подтвердил порог из ТЗ: **10 000 сотрудников, плавная прокрутка, основные экраны ≤ 1 с.** Текущая архитектура хранения этого не выдержит — задачи ниже надо закрыть **до** того, как база начнёт расти, иначе позже они потянут за собой переделку всех экранов.

**4.П.1. Убрать `persist` из `useDatabaseStore` — жёсткий блокер.**
Сейчас `useDatabaseStore` обёрнут в `persist({ name: 'hr-docs-local-db' })` без `partialize`, то есть **все шесть массивов сущностей целиком сериализуются в localStorage при каждой мутации**. Последствия на боевых объёмах:
- квота localStorage — около 5 МБ. 10 000 сотрудников это уже ~2–3 МБ JSON, а табели (10 000 × 12 месяцев в год, в каждом объект `days` на 28–31 ключ) — десятки мегабайт. Запись упадёт с `QuotaExceededError`;
- каждый клик по ячейке табеля вызывает `updateTimesheet` → полная пересериализация всего стора. При 10 000 строк это заметная блокировка UI на каждый клик;
- это вторая копия данных, которая уже сейчас создаёт риск рассинхрона: SQLite — источник истины, а localStorage переживает перезагрузку и может показать устаревшее до отработки `fetchAll()`. Именно этот механизм маскирует дефект импорта Excel (см. 4.0.8).

Убрать `persist` из `useDatabaseStore` полностью. Источник истины — SQLite, кэш — оперативная память. `useAppStore` (тема, акцент, плотность, шрифт, язык, валюта, сессия) `persist` **сохраняет** — там десяток скалярных полей, это правильный кандидат на localStorage.

**4.П.2. Загружать данные по срезу, а не всё сразу.**
`fetchAll()` тянет все шесть таблиц целиком при каждом входе. Для `employees`, `departments`, `positions`, `templates` это допустимо. Для `timesheets` и `archives` — нет: они растут линейно по времени и по штату. Ввести параметризованные выборки: `listTimesheets(year, month)` и `listArchives({ year, departmentId, employeeId })`, дёргать их при смене фильтра на экране. Оба слоя (`tauriDb.ts` и `server.ts`) правятся одинаково.

**4.П.3. Виртуализация списков.**
Добавить `@tanstack/react-virtual` (в проекте его нет) и применить:
- `Employees` — виртуализация строк таблицы TanStack Table;
- `Timesheet` — критичнее всего: 10 000 строк × 31 ячейка = 310 000 узлов DOM. Виртуализировать строки, sticky-колонки слева и справа сохранить;
- `Archive` и `Reports` — виртуализация либо пагинация, на выбор реализующего.

**4.П.4. Индексы в SQLite.**
Схема сейчас без единого индекса, кроме первичных ключей. Добавить в общий DDL (см. 4.4.1): `employees(department)`, `employees(status)`, `timesheets(year, month)`, `timesheets(employee_id)`, `archives(year)`, `archives(employee_id)`, `positions(department_id)`, `departments(parent_id)`.

**4.П.5. Поиск и фильтрация.**
Глобальный фильтр TanStack Table фильтрует массив в памяти — на 10 000 записей это работает, но с задержкой на каждый символ. Добавить debounce 200–300 мс. Если после виртуализации и debounce всё ещё медленно — перенести поиск в SQL (`LIKE` по `full_name`, `position`, `department`).

**4.П.6. Сид для нагрузочной проверки.**
Скрипт, наполняющий базу 10 000 сотрудников и годом табелей. Без него утверждение «≤ 1 с» непроверяемо. Замерять: холодный старт до `Dashboard`, открытие `Employees`, прокрутку, открытие `Timesheet`, экспорт в Excel.

> **Порядок важен:** 4.П.1 сделать до задач 4.1.x. Иначе пять модулей, переводимых на БД, добавят в localStorage ещё пять массивов и приблизят исчерпание квоты.

---

### Приоритет 1 — довести макетные экраны до реальных данных

Подтверждено решением заказчика: эти пять модулей — не демо, они входят в приёмку. Выполнять **после** 4.П.1, иначе пять новых массивов ускорят исчерпание квоты localStorage.

Для каждого: таблица в **обеих** схемах → методы в `src/data/index.ts` (обе ветки) → срез в `useDatabaseStore` → замена `useState` на стор. UI не переделывать — он уже готов.

| Экран | Новая таблица | Поля |
|---|---|---|
| **4.1.1** Recruiting | `candidates` | `id, full_name, position, experience, status, created_at, note` |
| **4.1.2** TimeOff | `time_off_requests` | `id, employee_id, type, date_from, date_to, days, status, comment` |
| **4.1.3** Onboarding | `checklist_tasks` | `id, employee_id, kind(onboarding\|offboarding), title, assignee, done, comment, order_index` |
| **4.1.4** Performance | `goals`, `reviews` | goals: `id, employee_id, title, progress, status, period`; reviews: `id, employee_id, reviewer, period, score, comment` |
| **4.1.5** KnowledgeBase | `kb_categories`, `kb_articles` | articles: `id, category_id, title, content_html, reads, created_at, updated_at` |

Дополнительно по каждому: убрать захардкоженные русские демо-данные из кода, счётчики и плитки-константы (12/48/8, «24 дня», «1 заявка», `onboarding.active`) считать из данных. В Recruiting добавить колонки `hired` и `rejected` — сейчас два статуса из шести недостижимы. В TimeOff заменить строковый ввод дат на `type="date"` (два поля) и считать `days` автоматически.

### Приоритет 2 — недостающие модули

**4.2.1. Движение кадров (`/movements`).**
Таблица `movements(id, employee_id, type, date, from_position, to_position, from_department, to_department, from_salary, to_salary, order_no, reason)`. Кнопки «Приём / Перевод / Увольнение» открывают формы (RHF + Zod, дровер как в `EmployeeForm`); проведение операции атомарно пишет запись в `movements` **и** обновляет `employees`. Работающие фильтры по типу и диапазону дат, экспорт в Excel. **Добавить пункт в сайдбар** — иначе экран недостижим.

**4.2.2. История изменений по сотруднику.**
Вкладка в дровере сотрудника: лента событий из `movements` + записи о правках карточки. Требование v1 «история/аудит по каждому сотруднику» не выполнено.

**4.2.3. Календарь (`/calendar`).**
Заменить статичную сетку на реальный расчёт месяца (`date-fns` уже в зависимостях). События: дни рождения (`employees.birth_date`), отпуска и больничные (`time_off_requests`), даты приёма (`employees.hire_date`). Навигация по месяцам, боковая панель «Ближайшие события» на реальных данных. **Добавить пункт в сайдбар.**

**4.2.4. Журнал аудита.**
Таблица `audit_log(id, ts, action, entity, entity_id, diff)`. Писать при: логине, смене пароля, CRUD сотрудников/подразделений/должностей/шаблонов, проведении кадровой операции, изменении настроек, сбросе данных. Заменить `mockAudit` в `Settings` на реальные записи с работающими фильтрами (тип действия / сущность / дата от) и экспортом.

**4.2.5. Генерация `.docx`.**
`src/lib/docx.ts` (docxtemplater + PizZip + file-saver) написан и никем не вызывается. Подключить в `DocumentGenerator`: кнопка «Скачать .docx» рядом с «Печать в PDF». Нужен `.docx`-бланк — хранить в `public/`, путь настраивать в «Настройки → Общие» (фирменный бланк). Данные для `doc.render()` собирать из тех же переменных, что уже собирает предпросмотр.

**4.2.6. Экспорт отчётов и табеля.**
Кнопка «Скачать Excel» в `Reports` и «Скачать» в `Timesheet` не подключены. Использовать существующий `exportToExcel` из `lib/excel.ts`. Для табеля — лист с днями месяца по колонкам и итогами.

**4.2.7. Конструктор отчётов.**
Вкладка «Конструктор (beta)» — сейчас плашка. Минимум: выбор источника (сотрудники / ШР / табель / архив), выбор полей чекбоксами, фильтры, группировка, сохранение пресета в новую таблицу `report_presets(id, name, config JSON)`, выгрузка в Excel и JSON.

### Приоритет 3 — данные, надёжность, настройки

**4.3.1. Резервное копирование.**
Заменить `mockBackups` реальным механизмом: экспорт всей БД в JSON-файл (все 7+ таблиц) и импорт с валидацией и подтверждением. Для Tauri — копия файла `.db` в выбранную папку. Список бэкапов, скачивание, удаление, восстановление.

**4.3.2. Многоуровневый сброс.**
Из v1 требование остаётся актуальным и не реализовано:
1. сброс настроек интерфейса к дефолтам (только `useAppStore`);
2. сброс настроек модуля к заводским (без удаления данных);
3. очистка данных отдельного модуля (выбор чекбоксами: кандидаты, заявки, чек-листы, статьи, архив, табели);
4. полный factory reset — двойное подтверждение (пароль + ввод фразы), обязательное предложение бэкапа, запись в аудит.

**4.3.3. Настройки → Общие: сделать рабочими.**
Поля организации (название, ИНН, руководитель), валюта, формат даты, стартовый экран. Хранить в таблице `app_settings(key, value)` или в `useAppStore` с персистом. Подставлять название организации в шапки документов и отчётов вместо захардкоженного «ООО "Глобал Тек"» (сейчас встречается в сиде и в стартовом шаблоне `TemplateBuilder`).

**4.3.4. Блокировка по бездействию.**
Таймер бездействия (настраиваемый, по умолчанию 15 мин) → экран блокировки с вводом пароля. Настройка во вкладке «Безопасность» вместо нерабочего тумблера «passwordless».

**4.3.5. Мастер импорта.**
Вкладка «Импорт/экспорт» — 4 шага только нарисованы. Реализовать: загрузка файла (xlsx/csv/xml) → маппинг колонок на поля сотрудника → предпросмотр с подсветкой ошибок валидации → импорт. `parseExcel` из `lib/excel.ts` уже готов. Заодно вернуть видимость кнопке импорта в `Employees` (снять класс `hidden`) или убрать её в пользу мастера.

**4.3.6. Профиль.**
Заменить моковые вкладки (расчётные листки, техника, задачи) реальными данными или удалить их. Кнопки «Редактировать профиль» и «Изменить пароль» — подключить (последнюю — переходом в Настройки → Пароль). Добавить смену языка и темы, как требовал v1.

**4.3.7. Убрать подсказку с паролем с экрана логина.**
`pages/Login.tsx`: предзаполненные `admin@global.tech / password123` и строка «Вход владельца: …» — оставить только на dev-сборке (`import.meta.env.DEV`). Локализовать оставшиеся русские строки экрана.

**4.3.8. Первичная настройка пароля.**
Сейчас пароль по умолчанию зашит в сид. Требование v1 «первичная настройка пароля при первом запуске» не выполнено: при пустой таблице `users` показывать экран создания пароля владельца вместо логина.

### Приоритет 4 — качество кода и UX

**4.4.1. Устранить дублирование схемы БД.**
Вынести DDL и сид в один разделяемый модуль (например, `src/data/schema.ts`), из которого оба слоя берут одинаковый список `CREATE TABLE`. Добавить примитивные версионированные миграции (таблица `_migrations`), чтобы `ALTER TABLE` не жил в `try/catch`.

**4.4.2. Включить строгий TypeScript.**
В `tsconfig.json` нет `strict`. Включить `strict: true`, `noUnusedLocals`, `noUnusedParameters` и починить ошибки. Заодно вычистить `any` в `tauriDb.ts`/`server.ts` (там их много) и неиспользуемые импорты (`Search`, `MoreHorizontal`, `Download` и др. импортируются, но не используются в нескольких файлах).

**4.4.3. Линтер и форматирование.**
В `d:\ОК` нет ни `.eslintrc`, ни `.prettierrc`. Добавить ESLint (typescript-eslint + react-hooks + jsx-a11y) и Prettier, добавить скрипты `lint:eslint` и `format`.

**4.4.4. Офлайн-шрифт.**
`src/index.css:1` тянет Inter с `fonts.googleapis.com` — в офлайн-сборке шрифт не загрузится. Положить `.woff2` в `public/fonts/` и подключить через `@font-face`.

**4.4.5. Тосты вместо `alert()`/`confirm()`.**
`alert`/`confirm` используются минимум в 10 файлах. Ввести лёгкий компонент тостов и модальное подтверждение (без внешних зависимостей либо на уже установленном `motion`, который сейчас в `package.json`, но нигде не импортируется). Заменить все вызовы.

**4.4.6. Состояния экранов.**
Требование v1 «загрузка / пусто / ошибка / успех» выполнено частично: пустые состояния есть в `Employees`, `Templates`, `Archive`, `OrgChart`; скелетонов загрузки и обработки ошибок нет нигде. `fetchAll()` при ошибке молча пишет в `console.error`. Добавить флаги `loading`/`error` в `useDatabaseStore`, скелетоны таблиц и карточек, экран ошибки с повтором.

**4.4.7. Корректность в минимальном окне (desktop-only).**
Мобильной и планшетной вёрстки **не делаем** — решение заказчика. Опорное разрешение 1280×800, минимальное окно 1024×700 (уже задано в `tauri.conf.json`). Существующие `md:`/`lg:`-утилиты (46 штук) не удалять — они безвредны и корректно отрабатывают при сужении окна.

Что проверить и починить именно при 1024×700:
- модалки `OrgChart`, `Recruiting`, `TimeOff`, `Performance`, `Onboarding` — задать `max-h-[85vh]` и внутренний скролл, сейчас длинные формы обрежутся;
- `Timesheet` и `Archive` — горизонтальный скролл уже есть, убедиться что sticky-колонки не наезжают друг на друга при 31-дневном месяце;
- `Settings` — при свёрнутом окне левая колонка вкладок (`w-64`) съедает половину ширины; сделать её `w-56` либо схлопывать в иконки;
- `Reports` — раскладка `w-1/3` + контент; при 1024 px правая часть становится теснее 600 px, проверить таблицы;
- сайдбар в свёрнутом состоянии (`w-20`) — рабочий сценарий для узкого окна, проверить что подписи не обрезаются криво.

Явно зафиксировать desktop-only в README, чтобы к вопросу не возвращались.

**4.4.8. Тесты.**

*Сделано 2026-08-23:* Vitest и Playwright установлены, контур описан в разделе 1.7. Закрыты API (19 тестов) и обход всех экранов (23 теста), `npm run verify` собирает всё в одну команду.

*Остаётся — покрыть расчётную логику,* она сейчас не проверяется ничем:
- итоги табеля (дни / явки / часы, автовыходные);
- ФОТ на `Dashboard` (`salary × rate`, поведение при пустом окладе);
- занято / вакантно в `OrgChart`;
- подстановка переменных в `DocumentGenerator`;
- `exportToExcel` / `parseExcel` — round-trip;
- паритет ключей ru/en/tk — проверкой автоматом, а не глазами.

Это чистые функции, им не нужны ни сервер, ни браузер: новый файл `tests/unit/*.test.ts` и расширение `include` в `vitest.config.ts`. Часть логики сейчас живёт прямо в компонентах — её придётся сначала вынести в `src/lib/`, и это самостоятельная польза от задачи.

*Не покрыто и требует отдельного решения:* ветка Tauri (`src/data/tauriDb.ts`). Веб-режим и нативный режим — два разных пути к данным, зелёный `verify` говорит только о первом.

**4.4.9. Почистить артефакты AI Studio.**
`README.md` — шаблон AI Studio про Gemini; `.env.example` описывает `GEMINI_API_KEY`, который нигде не читается; `@google/genai` и `motion` установлены и не используются; `metadata.json` и `OK.zip` в корне. Написать реальный README (запуск, сборка, структура), убрать мёртвые зависимости.

**4.4.10. Задействовать или удалить TanStack Query.**
Провайдер смонтирован в `main.tsx`, но `useQuery` нет ни в одном файле — данные тянет `fetchAll()` в `useDatabaseStore`. Либо перевести чтение на Query (кэш, инвалидация, `isLoading` бесплатно — хорошо ложится на задачу 4.4.6), либо удалить зависимость. Промежуточное состояние — мусор.

---

## 5. Критерии приёмки (обновлённые)

Приложение считается готовым, когда:

1. Собирается как Windows-инсталлятор (`nsis`/`msi`) через `npm run tauri:build` и работает **полностью офлайн** — без сети, внешнего сервера и внешних шрифтов.
2. Все данные лежат в локальной SQLite; ни один экран не хранит доменные данные только в `useState`.
3. Интерфейс полностью работает на tk / en / ru; в JSX нет захардкоженных русских строк (кроме доменных enum-кодов Т-13 и эндонимов языков).
4. Работают: кадровый учёт с историей движений, оргструктура с деревом, конструктор шаблонов, генерация документов в PDF **и** `.docx`, табель Т-13, отчёты с выгрузкой в Excel и JSON, архив, файловый импорт с маппингом.
5. Настройки: оформление (тема / акцент / плотность / шрифт), язык, реквизиты организации, валюта, смена пароля, блокировка по бездействию, бэкап и восстановление, многоуровневый сброс, реальный журнал аудита.
6. Светлая и тёмная темы одинаково корректны на всех экранах; цветовых хардкодов вне дизайн-токенов нет (кроме печатной «бумаги» в `DocumentGenerator`, категориальной палитры графиков и цветов текста в редакторе TipTap — это осознанные исключения).
7. **`npm run verify` проходит целиком:** `tsc --noEmit` с `strict: true` = 0 ошибок, ESLint = 0 ошибок, `vite build` проходит, API-тесты и e2e-обход всех экранов зелёные, юнит-тесты расчётной логики (4.4.8) зелёные. Ни один экран не выпадает из `tests/e2e/routes.ts`.
8. Ни один пункт сайдбара не ведёт на заглушку и ни один рабочий экран не остаётся без пункта сайдбара.
9. **Производительность на 10 000 сотрудников** (проверяется на сид-базе из 4.П.6): холодный старт до `Dashboard` ≤ 1 с, открытие `Employees` ≤ 1 с, прокрутка списка и табеля без видимых рывков, поиск отзывчив. `useDatabaseStore` не пишет в localStorage; `QuotaExceededError` невоспроизводим.
10. **Desktop-only:** при окне 1024×700 ни один экран не ломается, модалки скроллятся внутри себя, горизонтальные таблицы скроллятся без наложения sticky-колонок. Мобильная вёрстка в скоуп не входит и её отсутствие дефектом не считается.
11. **Валюта настраивается** из «Настройки → Общие» и единообразно применяется на всех экранах, в документах и во всех выгрузках. Символов `₽`/`₸` в коде не осталось.
