# Мастер-промпт для создания приложения
## «HR & Docs Platform» — пошаговая инструкция для ИИ-разработчика

> Используйте этот документ целиком как системный промпт для ИИ-агента разработки (Claude Code, Cursor и т. п.) либо как пошаговый план для команды. Выполняйте этапы строго по порядку; каждый шаг завершается работоспособным результатом и проверкой из блока «Готово, когда».

---

## 0. Роль и общие правила (читать первыми, соблюдать всегда)

Ты — senior full-stack инженер и продуктовый дизайнер. Твоя задача — построить десктопное (Windows/macOS/Linux) локальное однопользовательское приложение для кадрового учёта, управления штатным расписанием и генерации справок. Приложение рассчитано строго на одного работника: один пользователь, одно устройство, все данные хранятся локально, синхронизация по сети не предусмотрена. Приложение должно выглядеть современно и презентабельно — категорически не как «1С».

**Незыблемый стек:**
- Клиент: React 18 + TypeScript + Vite + Tailwind CSS.
- UI-компоненты: shadcn/ui (Radix UI).
- Десктоп-оболочка: Tauri 2.0.
- Состояние: Zustand (клиентское) + TanStack Query (серверное).
- Формы: React Hook Form + Zod.
- Локальная БД: SQLite (Tauri SQL plugin) — единственное хранилище данных на устройстве.
- i18n: i18next + react-i18next (языки: `tk`, `en`, `ru`).
- Таблицы: TanStack Table; графики: Recharts.
- Экспорт: ExcelJS (.xlsx), нативный JSON, docxtemplater (.docx); импорт Excel: SheetJS.
- Бэкенд: локальный, встроенный в приложение (например, Node/Express в составе десктоп-оболочки), работающий только с локальной SQLite. Внешний сетевой сервер, общая БД и удалённый доступ не используются — приложение полностью автономно на одном устройстве. Локальная авторизация: единственная учётная запись владельца устройства.

**Правила разработки:**
1. Строгий TypeScript (`strict: true`), никакого `any` без явного обоснования.
2. Единые Zod-схемы для валидации на клиенте и сервере.
3. Все пользовательские строки — только через i18n, никаких хардкод-текстов.
4. Каждый экран имеет состояния: загрузка (skeleton), пусто (empty state), ошибка, успех.
5. Доступность (a11y): семантика, фокус, контраст, работа с клавиатуры.
6. Атомарные коммиты, понятные имена, комментарии только там, где логика неочевидна.
7. После каждого этапа — краткий отчёт: что сделано, как проверить, что дальше.

---

## 1. Дизайн-система (задать до написания экранов)

Цель — чистый, «дышащий», презентабельный интерфейс уровня современных SaaS-продуктов.

**Шаг 1.1. Палитра.** Настрой токены в Tailwind:
- Нейтральная база: `slate` (фон, текст, границы).
- Один акцентный цвет: `indigo` (кнопки действия, активные состояния, ссылки).
- Семантика: success `emerald`, warning `amber`, danger `rose`, info `sky`.
- Полноценные светлая и тёмная темы через CSS-переменные.

**Шаг 1.2. Типографика.** Шрифт Inter (или системный). Чёткая иерархия: заголовки — плотный вес, тело — обычный, подписи — приглушённый `slate-500`. Табличные цифры (`tabular-nums`) для чисел и сумм.

**Шаг 1.3. Пространство и форма.** Скругления `rounded-xl` для карточек и кнопок, мягкие тени (`shadow-sm`/`shadow-md`), щедрые отступы, сетка на 4px. Никаких «серых рамочных» таблиц в стиле 1С — используй карточки, разделители-волоски, зебру только при необходимости.

**Шаг 1.4. Микровзаимодействия.** Плавные переходы (150–200 мс), hover/active-состояния, тосты для действий, скелетоны при загрузке, аккуратные анимации появления (без излишеств).

**Шаг 1.5. Каркас интерфейса:**
- Левый сайдбар с иконками и подписями (сворачиваемый).
- Верхняя панель: глобальный поиск, переключатель языка, тема, уведомления, меню пользователя.
- Контент — карточки и таблицы с виртуализацией.
- Модальные окна и правые выезжающие панели (drawer) для форм.

**Готово, когда:** есть работающий шаблон страницы с сайдбаром, топбаром, переключением тем и языка, и он выглядит презентабельно на десктопе при разных размерах окна.

---

## Этап 2. Инициализация проекта и инфраструктура

**Шаг 2.1.** Создай репозиторий: `apps/client` (React+Vite), `packages/shared` (общие типы и Zod-схемы). Отдельный сетевой сервер не заводится — бэкенд встроен в приложение и работает с локальной SQLite.

**Шаг 2.2.** Инициализируй клиент: Vite + React + TypeScript, подключи Tailwind CSS и настрой токены из этапа 1, установи shadcn/ui.

**Шаг 2.3.** Интегрируй Tauri 2.0 для десктоп-сборки под Windows/macOS/Linux; проверь запуск нативного окна.

**Шаг 2.4.** Подключи локальную SQLite (Tauri SQL plugin); создай схему таблиц и health-check локального бэкенда.

**Шаг 2.5.** Настрой ESLint/Prettier, Vitest, базовый CI (линт + тесты + сборка).

**Готово, когда:** десктоп-приложение собирается и запускается, локальная БД инициализируется, все операции выполняются офлайн на одном устройстве без обращения к сети.

---

## Этап 3. Локализация и локальный вход

**Шаг 3.1.** Подключи i18next; вынеси все строки в файлы `tk.json`, `en.json`, `ru.json`. Заполни переводы для всех трёх языков.

**Шаг 3.2.** Сделай переключатель языка в топбаре и **выбор языка по умолчанию**, который сохраняется в профиле пользователя и применяется при следующем входе.

**Шаг 3.3.** Настрой форматирование дат, чисел и валют под каждую локаль.

**Шаг 3.4.** Реализуй единственную локальную учётную запись владельца устройства: первичная настройка пароля при первом запуске, локальный вход и смена/сброс пароля. Регистрация новых пользователей и мультипользовательский режим не предусмотрены — приложение рассчитано на одного работника.

**Шаг 3.5.** Реализуй локальную блокировку приложения: вход по паролю, блокировка по бездействию. Ролей и разграничения прав нет — весь функционал доступен единственному пользователю.

**Шаг 3.6.** Сделай экран профиля (язык, тема, ФИО, фото).

**Шаг 3.7.** Заложи фундамент централизованного модуля настроек: единый реестр параметров, экран «Настройки» с разделами. Все параметры хранятся локально на устройстве и применяются к единственному пользователю.

**Готово, когда:** интерфейс полностью переключается между 3 языками, любой можно сделать языком по умолчанию, работает локальный вход единственного пользователя, открывается каркас «Настройки».

---

## Этап 4. Модуль кадрового учёта

**Шаг 4.1.** Спроектируй модель данных сотрудника и локальную таблицу SQLite.

**Шаг 4.2.** Сделай список сотрудников на TanStack Table: поиск, фильтры, сортировка, виртуализация, презентабельные карточки/строки.

**Шаг 4.3.** Реализуй карточку сотрудника (персональные данные, контакты, документы, фото) с формой на RHF + Zod.

**Шаг 4.4.** Реализуй кадровые операции: приём, перевод, изменение оклада, увольнение.

**Шаг 4.5.** Добавь историю изменений (аудит) по каждому сотруднику.

**Готово, когда:** можно завести, найти, отредактировать сотрудника и увидеть историю его кадровых событий; все данные хранятся локально на устройстве.

---

## Этап 5. Модуль штатного расписания (ШР)

**Шаг 5.1.** Реализуй дерево подразделений (иерархическая оргструктура) с наглядной визуализацией.

**Шаг 5.2.** Сделай справочники должностей, тарифных ставок и окладов.

**Шаг 5.3.** Реализуй штатные единицы: количество ставок, занятые/вакантные позиции, привязку сотрудников.

**Шаг 5.4.** Добавь версионирование ШР: утверждённые редакции с датами вступления в силу и сравнением версий.

**Шаг 5.5.** Сделай аналитику: сводка по ФОТ, индикаторы вакансий и переработок, дашборд на Recharts.

**Готово, когда:** можно построить оргструктуру, вести штатные единицы, утверждать версии ШР и видеть аналитику по ФОТ и вакансиям.

---

## Этап 6. Конструктор справок

**Шаг 6.1.** Сделай визуальный редактор шаблонов с drag-and-drop блоками: текст, таблицы, реквизиты, подписи.

**Шаг 6.2.** Реализуй поля-подстановки — вставку данных из карточки сотрудника и ШР (ФИО, должность, оклад, стаж, подразделение и т. д.).

**Шаг 6.3.** Добавь условную логику блоков (показ по условию).

**Шаг 6.4.** Создай библиотеку готовых шаблонов (справка с места работы, о доходах, о должности) и права доступа к ним.

**Шаг 6.5.** Реализуй предпросмотр справки с реальными данными выбранного сотрудника.

**Шаг 6.6.** Реализуй генерацию и сохранение готового документа; версионирование шаблонов.

**Готово, когда:** пользователь создаёт шаблон, подставляет данные сотрудника, видит предпросмотр и формирует готовую справку.

---

## Этап 7. Модуль отчётности (Excel, JSON)

**Шаг 7.1.** Сделай конструктор отчётов: выбор источника, полей, фильтров, группировок; сохраняемые пресеты.

**Шаг 7.2.** Добавь предустановленные отчёты (кадры, ШР, вакансии, ФОТ).

**Шаг 7.3.** Реализуй выгрузку в Excel (ExcelJS) с форматированием: заголовки, стили, автоширина, итоги, при необходимости формулы.

**Шаг 7.4.** Реализуй выгрузку в JSON по структурированной схеме.

**Шаг 7.5.** Вынеси генерацию тяжёлых отчётов в фоновый процесс на устройстве (например, Web Worker / фоновый поток локального бэкенда) с уведомлением о готовности; добавь локальный планировщик регулярных отчётов. Внешние очереди и сетевые сервисы не используются.

**Готово, когда:** любой отчёт строится и корректно выгружается в .xlsx и JSON.

---

## Этап 8. Файловый обмен данными (Excel, Word)

> Все обмены — только через локальные файлы. Никаких сетевых интеграций и удалённых серверов (в т. ч. живой обмен с 1С по OData/REST) — приложение работает 100% локально.

**Шаг 8.1.** Реализуй импорт данных из файлов (Excel через SheetJS, а также XML/CSV) с настраиваемым маппингом полей, валидацией и предпросмотром перед загрузкой.

**Шаг 8.2.** Реализуй экспорт данных в файлы (Excel/XML/CSV) для ручной передачи во внешние системы (например, в 1С), без прямого сетевого подключения.

**Шаг 8.3.** Реализуй генерацию Word (.docx) из шаблонов конструктора справок (docxtemplater) с поддержкой фирменного бланка (логотип, шапка, подписи).

**Готово, когда:** данные импортируются из локальных файлов и экспортируются в них, справки выгружаются в Word на фирменном бланке — всё без обращения к сети.

---

## Этап 9. Модуль настроек (полное наполнение)

Наполни центр настроек всеми разделами. Приложение однопользовательское и полностью локальное, поэтому все параметры хранятся на устройстве и применяются к единственному пользователю — без областей действия и без разграничения по ролям.

**Шаг 9.1.** Интерфейс: тема, акцент, плотность, размер шрифта, стартовый экран, состав меню.

**Шаг 9.2.** Язык и локализация: язык по умолчанию, форматы даты/чисел/валюты, часовой пояс, календарь.

**Шаг 9.3.** Профиль и аккаунт: данные владельца, смена пароля, политика паролей, блокировка по бездействию.

**Шаг 9.4.** Кадры: пользовательские поля карточки, справочники, обязательность и валидация.

**Шаг 9.5.** Штатное расписание: валюта и расчёт ФОТ, справочники должностей/надбавок, workflow утверждения, пороги вакансий.

**Шаг 9.6.** Конструктор справок: фирменный бланк, шрифты и стили по умолчанию, библиотека шаблонов, нумерация справок, блоки подписей/печатей.

**Шаг 9.7.** Отчёты и экспорт: формат по умолчанию (Excel/JSON), стили Excel, схема JSON, планировщик, пресеты.

**Шаг 9.8.** Файловый обмен: правила импорта Excel/XML/CSV и маппинг полей, шаблоны и бланк Word, каталоги по умолчанию для импорта/экспорта.

**Шаг 9.9.** Уведомления: внутренние уведомления приложения, события, режим «Не беспокоить». Без e-mail/push и любых внешних каналов.

**Шаг 9.10.** Данные и резервное копирование: расписание и глубина локальных бэкапов, восстановление, экспорт/импорт конфигурации, папка хранения на устройстве.

**Шаг 9.11. Сброс данных** — реализуй уровнями, от безопасного к необратимому, с подтверждением, записью в аудит и предложением бэкапа:
- 9.11.1. Сброс пользовательских настроек к значениям по умолчанию.
- 9.11.2. Сброс настроек модуля/системы к заводским (без удаления данных).
- 9.11.3. Очистка данных отдельного модуля (например, черновики справок, временные отчёты).
- 9.11.4. Полный сброс системы (factory reset) — необратимо, двойное подтверждение (пароль + фраза), обязательный бэкап, запись в аудит.

**Шаг 9.12.** Аудит и система: детализация и срок хранения журнала, шифрование локальной БД, блокировка по бездействию, обновления, сведения о системе.

**Готово, когда:** все настраиваемые функции доступны из единого модуля настроек и корректно применяются; работают все уровни сброса данных с защитой.

---

## Этап 10. Тестирование, безопасность, приёмка

**Шаг 10.1.** Покрой код модульными тестами (Vitest), логику локального бэкенда — интеграционными, сценарии — e2e (Playwright).

> **Статус на 2026-08-23:** выполнено частично. Интеграционные тесты локального API (19, Vitest) и сценарные e2e (23, Playwright — обход всех 18 экранов) написаны и зелёные; полная проверка — `npm run verify`. Не покрыта расчётная логика (табель, ФОТ, занятость ШЕ, подстановка переменных, Excel round-trip, паритет ключей ru/en/tk) и ветка Tauri. Актуальная постановка — `BUILD_PROMPT.v2.md`, разделы 1.7 и 4.4.8.

**Шаг 10.2.** Проведи нагрузочное тестирование выгрузки отчётов и работы с большими объёмами локальных данных.

**Шаг 10.3.** Выполни аудит безопасности: шифрование локальной БД, защита ПДн на устройстве, журнал аудита.

**Шаг 10.4.** Протестируй на всех целевых платформах (Windows/macOS/Linux) и проверь все три локализации.

**Шаг 10.5.** Проведи пользовательское приёмочное тестирование (UAT).

**Готово, когда:** тесты зелёные, безопасность проверена, приёмка пройдена.

---

## Этап 11. Сборка, развёртывание, документация

**Шаг 11.1.** Настрой инсталляторы: Windows (`.msi`/`.exe`), macOS (`.dmg`), Linux (`.AppImage`/`.deb`).

**Шаг 11.2.** Настрой механизм автообновления десктоп-приложения (Tauri updater) и подпись сборок.

**Шаг 11.3.** Настрой локальное резервное копирование БД (папка на устройстве) и проверь восстановление из бэкапа.

**Шаг 11.4.** Подготовь руководство пользователя (3 языка).

**Готово, когда:** приложение устанавливается на всех платформах, локальные бэкапы и восстановление работают, документация готова.

---

## Финальный критерий приёмки всего проекта

Приложение собирается как десктоп-приложение (Windows/macOS/Linux) и работает 100% локально на одном устройстве под единственным пользователем, без сети, внешнего сервера и синхронизации; все данные хранятся в локальной SQLite; интерфейс работает на туркменском, английском и русском с выбором языка по умолчанию; работают штатное расписание с версиями, конструктор справок, выгрузка отчётов в Excel и JSON, файловый импорт/экспорт (Excel/XML/CSV) и генерация Word, централизованный модуль настроек с многоуровневым сбросом данных; дизайн современный, презентабельный и не похож на «1С».
