# Аудит ТЗ против фактического кода

**Объект:** `TZ.md` v1.0 · **Проект:** `d:\ОК` (HRDesk) · **Дата среза:** 2026-08-01

**Легенда:**
`A` соответствует · `B` в проекте лучше · `C` реализовано иначе · `D` частично · `E` отсутствует · `F` устарело · `G` конфликтует с проектом

**Правило разрешения:** при `B`, `C`, `G` прав код — правится ТЗ. При `D`, `E` прав ТЗ — работа остаётся в плане. При `F` пункт удаляется.

---

## Раздел 1. Общие сведения

| № | Пункт ТЗ | Ст. | Факт |
|---|---|---|---|
| 1.1 | Назначение: кадровый учёт, ШР, справки; интерфейс, отличный от «1С» | **A** | Выполнено. Дизайн-система на 8 уровней фона, 6 акцентов, светлая/тёмная тема |
| 1.2а | Платформы Windows / macOS / Linux | **G** | `tauri.conf.json` → `"targets": ["nsis","msi"]`. Только Windows. Нужно решение: либо добавить таргеты, либо зафиксировать Windows-only |
| 1.2б | 100% локально, офлайн, однопользовательский | **D** | Архитектурно да. Но `src/index.css:1` тянет Inter с `fonts.googleapis.com` — единственный внешний запрос, в офлайне шрифт не загрузится |
| 1.2в | Языки tk / en / ru | **A** | 3 локали, ~440 ключей, паритет подтверждён |
| 1.2г | Язык по умолчанию выбирается и сохраняется | **A** | `useAppStore.language` + `persist` + `onRehydrateStorage → i18n.changeLanguage` |
| 1.2д | Выгрузка отчётов: Excel **и JSON** | **D** | Excel есть (`lib/excel.ts`, `Archive`). JSON-выгрузки нет нигде |
| 1.2е | Обмен: Excel / XML / CSV / Word | **D** | Только Excel. XML и CSV отсутствуют полностью. Word — см. 2.3в |
| 1.2ж | Стек React + TS + Vite + Tailwind | **A** | Ядро совпадает |

---

## Раздел 2. Стек технологий

### 2.1. Клиент

| № | Пункт ТЗ | Ст. | Факт |
|---|---|---|---|
| 2.1а | **React 18** | **G** | Стоит React **19.0.1**. Прямое противоречие, прав проект |
| 2.1б | Vite | **A** | Vite 6 |
| 2.1в | Tailwind CSS | **B** | Tailwind **v4**, CSS-first через `@theme` в `index.css`, без `tailwind.config.js`. Новее, чем подразумевало ТЗ |
| 2.1г | **shadcn/ui (Radix UI)** | **G** | Не установлен и не используется. Все 19 экранов — собственная разметка на токенах. Внедрение = переписать весь UI ради нулевой пользы |
| 2.1д | Tauri 2.0 | **A** | `src-tauri/`, Tauri 2, `hrdesk`, id `com.hrdesk.local` |
| 2.1е | Zustand | **B** | 2 стора с `persist`; `useAppStore` дополнительно применяет тему синхронным IIFE до гидратации (защита от вспышки) |
| 2.1ж | **TanStack Query** для кэша и async | **C** | Установлен, `QueryClientProvider` смонтирован в `main.tsx`, но **`useQuery` не вызывается нигде**. Данные тянет `fetchAll()` в `useDatabaseStore`. Либо задействовать, либо удалить — текущее состояние мусорное |
| 2.1з | React Hook Form + Zod | **D** | Только в `EmployeeForm` (с локализованными сообщениями). Все остальные формы — «сырые» `useState` + `required` |
| 2.1и | SQLite через Tauri SQL plugin | **A** | `@tauri-apps/plugin-sql`, preload `sqlite:local-hr-docs.db` |
| 2.1к | i18next + react-i18next | **B** | Локали — **TS-модули**, а не JSON: типобезопаснее, попадают в бандл, гарантированно работают офлайн |
| 2.1л | Recharts / Tremor | **A** | Recharts в `Dashboard`, оси и тултип перекрашены токенами через CSS |
| 2.1м | TanStack Table, **виртуализация** | **D** | Таблица только в `Employees` (сортировка + глобальный поиск). Виртуализации нет ни одной. В `Timesheet`, `Reports`, `Archive`, `Movements` — обычные `<table>` |

### 2.2. Локальный бэкенд

| № | Пункт ТЗ | Ст. | Факт |
|---|---|---|---|
| 2.2а | **Node.js + Express, встроенный в десктоп-оболочку** | **C** | Express существует (`server.ts` + `better-sqlite3`), но **только как dev-обвязка с Vite middleware**. В Tauri-сборке его нет: фронтенд ходит в SQLite напрямую через `data/tauriDb.ts`. Ветвление в `data/index.ts` по `isTauri`. Это проще и надёжнее «встроенного сервера» — ТЗ переписать под фактическую схему |
| 2.2б | Локальная SQLite как единственное хранилище | **D** | Да для 6 сущностей. Но 5 экранов (`Recruiting`, `TimeOff`, `Onboarding`, `Performance`, `KnowledgeBase`) держат данные в `useState` и в БД не пишут |
| 2.2в | Аутентификация локальным паролем владельца | **A** | bcryptjs, единственная запись в `users`, роль `ADMIN` |
| 2.2г | Фоновые задачи (Web Worker) | **E** | Нет. Для объёмов одного предприятия избыточно — рекомендую перевести в «опционально» |

### 2.3. Библиотеки экспорта

| № | Пункт ТЗ | Ст. | Факт |
|---|---|---|---|
| 2.3а | ExcelJS | **A** | `lib/excel.ts` → `exportToExcel`, жирная шапка, ширина колонок |
| 2.3б | SheetJS для импорта | **D** | `parseExcel` написан и работает. Но кнопка импорта в `Employees` скрыта классом `hidden`, а сам импорт пишет через `setEmployees()` — **в БД не попадает** (см. дефекты) |
| 2.3в | **docxtemplater + docx** | **D** | `lib/docx.ts` (docxtemplater + PizZip + file-saver) написан и **ни одной страницей не импортируется**. Пакет `docx` не установлен вовсе |
| 2.3г | Экспорт JSON | **E** | Отсутствует |
| 2.3д | Обмен только файловый, без сети | **A** | Соблюдается |

---

## Раздел 3. Обзор модулей

| № | Модуль ТЗ | Ст. | Факт |
|---|---|---|---|
| 3.1 | Кадровый учёт: карточки, приём/перевод/увольнение, история | **D** | Карточки и CRUD есть. Кадровых операций и истории нет — `/movements` заглушка |
| 3.2 | ШР: структура, должности, ставки, вакансии, **версии** | **D** | Подразделения, должности, занято/вакантно — есть. Дерева нет (плоский список), версий нет |
| 3.3 | Конструктор справок | **D** | Шаблоны, блоки, подстановки, предпросмотр, печать — есть. DnD, условная логика, версии — нет |
| 3.4 | Отчётность Excel + JSON | **D** | 5 преднастроенных отчётов (2 на реальных данных). Конструктора нет, кнопка выгрузки не подключена, JSON нет |
| 3.5 | Файловый обмен Excel/XML/CSV + Word | **D** | Только Excel-экспорт |
| 3.6 | Администрирование: язык, пароль, **журнал аудита** | **D** | Язык и пароль есть. Журнал аудита — `mockAudit`, 3 захардкоженные строки |
| 3.7 | Централизованный модуль настроек, **единый реестр параметров** | **D** | Экран из 7 вкладок есть. Реестра как архитектурной сущности нет: оформление живёт в `useAppStore`, остальное — нигде. Реквизиты организации не сохраняются |

---

## Раздел 4. Этапы

### Этап 0 — обследование и архитектура

| Пункт | Ст. | Факт |
|---|---|---|
| Интервью, сбор требований | **F** | Этап пройден де-факто, продукт построен |
| Проектирование модели данных | **D** | Модель существует в коде (7 таблиц), но **описана дважды** — `src/db/sqlite.ts` и `src/data/tauriDb.ts`, и уже разошлась (`ALTER TABLE` в одном, `CREATE TABLE` в другом). ER-документации нет |
| Проектирование локальной архитектуры | **A** | Реализована и работает |
| Дизайн-система, токены | **B** | Сильно превосходит ТЗ: 8 уровней фона, 3 границы, 4 текста, 6 акцентов, 3 плотности, 3 размера шрифта, печатные стили |
| Figma-макеты, ER-диаграмма | **F** | Артефакты «до разработки». Заводить сейчас смысла нет; полезно только описание схемы БД |

### Этап 1 — инфраструктура

| Пункт | Ст. | Факт |
|---|---|---|
| Репозиторий клиент + бэкенд | **C** | Плоская структура `src/` + `server.ts` + `src-tauri/`, без монорепо и `packages/shared` |
| **CI/CD** | **E** | Отсутствует |
| Vite + React + TS + Tailwind + shadcn/ui | **D** | Всё кроме shadcn/ui (см. 2.1г) |
| Tauri под Win/macOS/Linux | **D** | Только Windows-таргеты |
| Схема таблиц + **миграции** | **D** | Схема есть, версионированных миграций нет — `ALTER TABLE` в `try/catch` |
| Каркас навигации, темы, лейаут | **B** | Сайдбар со сворачиванием, 14 пунктов, полностью локализован; тем больше, чем требовалось |

### Этап 2 — мультиязычность и локальный вход

| Пункт | Ст. | Факт |
|---|---|---|
| i18next, вынос **всех** строк | **D** | UI-хром переведён полностью. Хардкод русского остался в: `Login` («Пароль», «Войти», подзаголовок), демо-данных `Recruiting`, `Onboarding`, `KnowledgeBase`, `Performance`, `Profile`, событиях `Dashboard` |
| Полный перевод на 3 языка | **D** | Паритет 440 ключей подтверждён программно. Туркменский — машинного качества, носительская вычитка не проведена (зафиксировано в `AUDIT_REPORT.md`) |
| Переключатель языка, язык по умолчанию | **A** | Работает, сохраняется между сессиями |
| **Форматы даты и чисел по локали** | **E** | Не реализовано. В коде: сырые ISO-строки (`Reports`), `toLocaleString()` без локали (`OrgChart`, `Archive`), жёстко `'ru-RU'` (`Employees`), названия месяцев из i18n-массива |
| **Направление текста (RTL)** | **F** | tk / en / ru — все LTR. Требование неприменимо |
| **Первичная настройка пароля при первом запуске** | **E** | Пароль зашит в сид (`password123`). Экрана создания пароля нет |
| Вход и смена пароля | **A** | Работают через `api.login` / `api.changePassword` |
| **Сброс пароля** | **E** | Механизма восстановления нет |
| Профиль (язык, тема, ФИО) | **D** | `/profile` показывает имя и email. Смены языка, темы и фото на экране нет. Кнопки «Редактировать профиль» и «Изменить пароль» — пустышки |
| Фундамент централизованного модуля настроек | **D** | См. 3.7 |
| **Критерий: непереведённых строк нет** | **D** | Не достигнут — см. первую строку этапа |

### Этап 3 — кадровый учёт

| Пункт | Ст. | Факт |
|---|---|---|
| Карточка: персональные данные | **D** | Есть: ФИО, должность, подразделение, статус, даты приёма и рождения, тип оплаты, оклад, ставка |
| Карточка: **контакты, документы, фото** | **E** | Полей нет ни в схеме, ни в форме |
| Список: поиск, сортировка | **A** | TanStack Table, глобальный фильтр, сортировка по клику |
| Список: **фильтры** | **G** | Три селекта (подразделение / статус / ставка) и «Сбросить» нарисованы, но ни к чему не подключены — интерфейс обещает функцию, которой нет |
| Список: виртуализация | **E** | Нет |
| **Кадровые операции: приём, перевод, изменение оклада, увольнение** | **E** | `/movements` — заглушка с текстом «Загрузка…», таблицы `movements` в БД нет, пункта в сайдбаре нет |
| **История изменений по сотруднику (аудит)** | **E** | Отсутствует |
| Защита ПДн | **E** | БД не шифруется, блокировки нет |

### Этап 4 — штатное расписание

| Пункт | Ст. | Факт |
|---|---|---|
| **Иерархия подразделений (дерево)** | **D** | `departments.parent_id` в схеме есть, CRUD работает. Но `OrgChart.tsx:103` фильтрует `d.parentId === 'd1' \|\| d.parentId !== null` — условие пропускает любой узел с родителем, вложенность не рендерится, список плоский |
| Справочник должностей | **A** | `positions` (title, max_count, salary), CRUD через модалки |
| **Справочник тарифных ставок и надбавок** | **E** | Отсутствует |
| Штатные единицы: занято / вакантно | **A** | Считается корректно: `max_count` минус фактические сотрудники |
| **Привязка сотрудников к штатным единицам** | **C** | Связи по FK нет. `employees.position` и `.department` — свободный текст, сопоставление идёт сравнением строк (`e.position === pos.title`). Работает, но ломается при переименовании. ТЗ описать как есть + вынести переход на FK в задачи |
| **Версионирование ШР** | **E** | Кнопка «Новая редакция» есть, обработчика нет |
| Визуализация оргструктуры (дерево / диаграмма) | **D** | Список + таблица должностей. Диаграммы нет |
| Сводка по ФОТ | **D** | Плитка на `Dashboard` (`salary × rate`, при пустом окладе подставляется 85000). Разреза по подразделениям в `OrgChart` нет |
| Индикаторы вакансий | **D** | В `OrgChart` — реальные. Плитка «Вакансии» на `Dashboard` — формула-заглушка `max(len+4,10) − len` |
| Индикаторы **переработок** | **E** | Отсутствуют, хотя часы в `Timesheet` считаются |

### Этап 5 — конструктор справок

| Пункт | Ст. | Факт |
|---|---|---|
| Визуальный редактор блоков | **D** | Блоки text / table / signature, добавление и удаление, редактирование имени, сохранение в БД, печать |
| **Drag-and-drop перестановка блоков** | **E** | Нет, хотя `@hello-pangea/dnd` уже в проекте (используется в `Recruiting`) |
| Блок «реквизиты» | **E** | Типа блока нет. `table` рендерится фиксированной заглушкой «Показатель / Значение» |
| Поля-подстановки | **A** | `{{fullName}}`, `{{position}}`, `{{department}}`, `{{hireDate}}`, `{{salary}}` + русские алиасы `ФИО` / `Должность` / `Отдел`, палитра с копированием в буфер |
| **Условная логика блоков** | **E** | Отсутствует |
| Библиотека готовых шаблонов | **D** | В сиде один шаблон — «Справка с места работы». «О доходах» и «О занимаемой должности» нет |
| Предпросмотр с реальными данными | **A** | `DocumentGenerator`: выбор шаблона и сотрудника, подстановка, вёрстка «на бумаге» |
| Генерация и **сохранение** документа | **D** | Печать в PDF работает. `.docx` не подключён. Реестра выданных справок нет |
| **Версионирование шаблонов** | **E** | Отсутствует |
| **Права доступа к шаблонам** | **F** | Пользователь один, роль одна. Противоречит разделу 6 самого ТЗ |

### Этап 6 — отчётность

| Пункт | Ст. | Факт |
|---|---|---|
| **Конструктор отчётов** | **E** | Вкладка «Конструктор (beta)» — плашка с текстом |
| Предустановленные отчёты | **D** | 5 штук. На реальных данных: «Списочный состав» и частично «Штатная расстановка» (колонки «Штатных единиц» = `count+1`, «Вакантно» = константа 1). Мок: «Текучесть» (4.2% / 12), «Табель» (сгенерированные 15 дней), «Движения» (фейковые номера приказов) |
| Выгрузка в Excel с форматированием | **D** | `exportToExcel` работает в `Employees`, `Archive`. Кнопка «Скачать Excel» в `Reports` и «Скачать» в `Timesheet` **не подключены** |
| **Выгрузка в JSON** | **E** | Отсутствует |
| Фоновая генерация (Web Worker) | **E** | Нет. Рекомендую понизить до «опционально» |
| Планировщик регулярных отчётов | **E** | Нет (в ТЗ уже помечен «опционально») |

### Этап 7 — файловый обмен

| Пункт | Ст. | Факт |
|---|---|---|
| Импорт Excel | **D** | `parseExcel` работает. Но: кнопка скрыта классом `hidden`; маппинг захардкожен (`row['ФИО'] \|\| row['fullName']`); **результат пишется через `setEmployees()`, который не вызывает API — данные в SQLite не попадают** |
| Импорт XML / CSV | **E** | Отсутствует |
| Валидация и предпросмотр перед загрузкой | **E** | Отсутствуют |
| **Маппинг полей, настраиваемый в интерфейсе** | **E** | Вкладка «Импорт/экспорт» в Настройках — мастер из 4 шагов, только вёрстка |
| Экспорт в Excel | **A** | Работает |
| Экспорт в XML / CSV | **E** | Отсутствует |
| Генерация `.docx` | **D** | Модуль написан, в UI не подключён |
| **Фирменный бланк (логотип, шапка, подписи)** | **E** | Отсутствует. В шаблонах захардкожено «ООО "Глобал Тек"» |

### Этап 8 — тестирование и безопасность

| Пункт | Ст. | Факт |
|---|---|---|
| Модульные тесты (Vitest) | **D** | Vitest установлен (2026-08-23), 19 тестов в `tests/api/`. Покрыто API; расчётная логика (табель, ФОТ, занятость ШЕ, подстановка переменных, Excel round-trip, паритет ключей) **не покрыта** — остаётся задачей 4.4.8 |
| Интеграционные тесты бэкенда | **A** | `tests/api/auth.test.ts` + `crud.test.ts`: авторизация, смена пароля, CRUD по всем шести сущностям. Поднимают Express в процессе теста через `app.listen(0)`, работают на изолированной БД |
| **e2e (Playwright)** | **A** | ~~F~~ → **A**. Вердикт «несоразмерно» пересмотрен 2026-08-23, основание — ниже, в разделе «Пересмотр от 2026-08-23». `tests/e2e/`: 23 теста, обход всех 18 экранов, вход, навигация по сайдбару |
| **Нагрузочное тестирование** | **F** | Остаётся снятым. Порог 10 000 сотрудников проверяется сид-базой и замером сценариев (`BUILD_PROMPT.v2.md`, Приоритет 0.5), а не нагрузочным стендом — пользователь на устройстве один |
| **Шифрование локальной БД** | **E** | Файл `.db` лежит открытым |
| Защита ПДн | **E** | Нет |
| Журнал аудита | **E** | `mockAudit`, 3 строки |
| Тестирование на всех платформах | **G** | Собирается только под Windows |
| Проверка трёх локализаций | **D** | Паритет ключей проверен программно. Носительская вычитка туркменского не выполнена |
| UAT | **E** | Не проводилось |

### Этап 9 — развёртывание

| Пункт | Ст. | Факт |
|---|---|---|
| Инсталляторы Windows | **A** | `nsis` + `msi` настроены |
| Инсталляторы macOS / Linux | **D** | Таргеты не заданы. Требует решения — см. 1.2а |
| **Tauri updater, автообновление, подпись** | **F** | Прямо противоречит требованию «100% офлайн, без сети» из того же ТЗ |
| **Локальное резервное копирование и восстановление** | **E** | `mockBackups`, все кнопки-пустышки |
| Руководство пользователя на 3 языках | **E** | Отсутствует. `README.md` — шаблон AI Studio про Gemini |
| Обучение, передача, гарантия | **E** | Организационная часть, вне кода |

---

## Раздел 5. Нефункциональные требования

| Требование | Ст. | Факт |
|---|---|---|
| Экраны ≤ 1 с; 10 000 сотрудников плавно | **E** | Не измерялось, виртуализации нет. Для реального штата предприятия порог в 10 000 избыточен — рекомендую снизить до 2 000 или убрать |
| 100% работа без сети | **D** | Единственное нарушение — внешний `@import` шрифта Inter |
| Шифрование БД, блокировка по бездействию, аудит | **E** | Ни один из трёх пунктов не реализован |
| Кроссплатформенность Win / macOS / Linux | **G** | Только Windows |
| Локализация 3 языка + язык по умолчанию | **B** | Выполнено, плюс переключение на лету без перезагрузки |
| Модульная архитектура | **B** | Фасад данных с двумя реализациями, единые токены, изолированные страницы — расширяется хорошо |
| UX, отличный от «1С» | **B** | Существенно превосходит: 6 акцентов, 3 плотности, 3 размера шрифта, полноценные обе темы |
| Файловый обмен без потери данных | **D** | Только Excel-экспорт проверен |

---

## Раздел 6. Пользователь системы

| Пункт | Ст. | Факт |
|---|---|---|
| Один пользователь, роль владельца, без RBAC | **A** | Соблюдается всюду: `Role = 'ADMIN'`, комментарии в коде фиксируют решение, сайдбар без фильтрации по правам |

---

## Раздел 7. Критерии приёмки

| Критерий | Ст. |
|---|---|
| Собирается как десктоп из единой кодовой базы | **D** — только Windows |
| Работает 100% локально, данные в SQLite | **D** — 5 экранов не пишут в БД; внешний шрифт |
| 3 языка, любой как язык по умолчанию | **A** |
| ШР с версионированием и вакансиями | **D** — вакансии есть, версий нет |
| Конструктор справок формирует справку с данными | **A** |
| Отчёты в Excel **и JSON** | **D** — JSON нет, кнопка Excel в Reports не подключена |
| Импорт Excel/XML/CSV, экспорт, генерация Word | **D** — только Excel-экспорт |
| Интерфейс отличается от «1С» | **A** |
| Все настройки в едином модуле, применяются корректно | **D** — работает только вкладка «Оформление» + смена пароля + сброс |

---

## Приложение А. Модуль настроек

### А.1. Интерфейс и внешний вид

| Пункт | Ст. | Факт |
|---|---|---|
| Тема светлая / тёмная / системная | **A** | Работает, персистится, применяется до гидратации |
| Акцентный цвет | **B** | 6 палитр вместо одного цвета |
| Плотность (компактный / комфортный) | **B** | 3 уровня — compact / standard / spacious, управляют `--table-p` и `--table-text` |
| Размер шрифта | **A** | 3 уровня через `--app-font-size` на `html` |
| Параметры доступности (контраст, снижение анимаций) | **E** | Нет |
| Стартовый экран | **E** | Жёстко `/dashboard` |
| Состав бокового меню (закрепление модулей) | **E** | Список `navItems` захардкожен |
| Формат списков (таблица / карточки) | **G** | В `Employees` нарисованы две кнопки-переключателя вида — не работают |
| Число строк на странице | **E** | Пагинации нет ни на одном экране |

### А.2. Язык и локализация

| Пункт | Ст. | Факт |
|---|---|---|
| Язык по умолчанию tk / en / ru | **A** | Работает |
| Формат даты и времени, часовой пояс | **E** | Нет |
| Формат чисел, разделитель, **символ и позиция валюты** | **G** | Настройки нет, и валюта в коде **несогласованна**: `₽` в `Employees`, `₸` в `OrgChart` и `Archive` |
| Первый день недели, календарь, праздники | **E** | Нет. `CalendarView` статичен |

### А.3. Профиль и аккаунт

| Пункт | Ст. | Факт |
|---|---|---|
| ФИО, фото, контакты, подпись / факсимиле | **E** | Только имя и email из сессии |
| Смена пароля | **A** | Реальная, через `api.changePassword` |
| Политика паролей (длина, сложность, срок) | **D** | Минимум 6 символов захардкожен в `Settings.tsx`; настраиваемости нет |
| Блокировка по бездействию | **E** | Во вкладке «Безопасность» есть неработающий тумблер «passwordless» вместо этого |

### А.4. Кадровый модуль

| Пункт | Ст. | Факт |
|---|---|---|
| Пользовательские поля карточки | **E** | Набор полей фиксирован |
| Справочники: типы документов, причины приёма / увольнения, категории | **E** | Нет |
| Обязательность полей и правила валидации | **D** | Zod-схема в `EmployeeForm` работает, но зашита в код |

### А.5. Штатное расписание

| Пункт | Ст. | Факт |
|---|---|---|
| Валюта, параметры расчёта ФОТ, округление | **E** | Нет |
| Справочники должностей | **A** | CRUD в `OrgChart` |
| Справочники тарифных сеток и надбавок | **E** | Нет |
| Workflow утверждения версий ШР | **E** | Нет |
| Пороги индикаторов вакансий и переработок | **E** | Нет |

### А.6. Конструктор справок

| Пункт | Ст. | Факт |
|---|---|---|
| Фирменный бланк: логотип, шапка, колонтитулы, реквизиты | **E** | Нет |
| Шрифты, стили и поля документа по умолчанию | **E** | Стили печати зашиты в `DocumentGenerator` |
| Управление библиотекой шаблонов | **A** | `Templates`: сетка, поиск, создание, редактирование, удаление |
| Нумерация и регистрация выданных справок | **E** | Нет |
| Блоки подписей и печатей, факсимиле | **D** | Блок `signature` в конструкторе есть, настроек и факсимиле нет |

### А.7. Отчёты и экспорт

| Пункт | Ст. | Факт |
|---|---|---|
| Формат экспорта по умолчанию | **E** | Настройки нет |
| Стили Excel по умолчанию | **D** | Зашиты в `lib/excel.ts`: жирная шапка, ширина 20 |
| Схема JSON-выгрузки | **E** | JSON нет |
| Планировщик регулярных отчётов | **E** | Нет |
| Сохранённые пресеты отчётов | **E** | Нет |

### А.8. Файловый обмен

| Пункт | Ст. | Факт |
|---|---|---|
| Правила импорта и маппинг колонок | **E** | Мастер из 4 шагов — только вёрстка |
| Каталоги по умолчанию для экспорта | **E** | Браузерное «Скачать» |
| Word: шаблоны, бланк, подстановки по умолчанию | **E** | Нет |

### А.9. Уведомления

| Пункт | Ст. | Факт |
|---|---|---|
| Внутренние уведомления вкл. / выкл. | **E** | Системы уведомлений нет — вместо неё `alert()` минимум в 10 файлах |
| События для оповещения | **E** | Нет |
| Режим «Не беспокоить» | **E** | Нет |

### А.10. Данные и резервное копирование

| Пункт | Ст. | Факт |
|---|---|---|
| Расписание и глубина бэкапа | **E** | `mockBackups` |
| Восстановление из копии | **E** | Кнопки-пустышки |
| Экспорт / импорт настроек | **E** | Нет |
| Папка хранения данных | **E** | Показан статичный текст `/app/backups` |

### А.10.1. Сброс данных — четыре уровня

| Уровень | Ст. | Факт |
|---|---|---|
| 1. Сброс пользовательских настроек к дефолтам | **E** | Нет |
| 2. Сброс настроек модуля к заводским | **E** | Нет |
| 3. Очистка данных отдельного модуля | **E** | Нет |
| 4. Полный factory reset | **G** | Есть `resetSystem` с проверкой пароля и очисткой localStorage, но: **удаляет только `employees` и `templates` из 7 таблиц** (остаются departments, positions, timesheets, archives, users) — то есть не является полным сбросом, вопреки названию. Нет второго подтверждения фразой, нет обязательного бэкапа, нет записи в аудит |

### А.11. Аудит, безопасность, система

| Пункт | Ст. | Факт |
|---|---|---|
| Детализация и срок хранения журнала аудита | **E** | Журнала нет |
| Шифрование локальной БД | **E** | Нет |
| Политика блокировки при бездействии | **E** | Нет |
| **Проверка и установка обновлений, канал обновлений** | **F** | Противоречит «100% офлайн» |
| Сведения о системе, версия, лицензия | **D** | Вкладка «Лицензия» существует и пуста (`settings.license.loading`) |

---

## Чего в ТЗ нет, но оно есть в проекте

Семь модулей построены сверх ТЗ. Их нужно **внести в ТЗ**, а не удалять из проекта.

| Модуль | Маршрут | Состояние |
|---|---|---|
| Рекрутинг — канбан кандидатов с drag-and-drop | `/recruiting` | UI готов, данные в `useState` |
| Отсутствия — заявки на отпуск / больничный, approve-reject | `/timeoff` | UI готов, данные в `useState` |
| Адаптация / Offboarding — чек-листы задач | `/onboarding` | UI готов, данные в `useState` |
| Оценка — цели, прогресс, ревью 360° | `/performance` | UI готов, данные в `useState` |
| База знаний — статьи, категории, TipTap-редактор | `/knowledge-base` | UI готов, данные в `useState` |
| Архив — данные прошлых периодов, экспорт Excel / PDF / печать | `/archive` | Работает на реальных данных |
| Табель Т-13 — 10 буквенных кодов, sticky-колонки, итоги | `/timesheet` | Работает, сохраняется в БД |
| Профиль пользователя | `/profile` | Макет |

---

## Дефекты, найденные при сверке

| Место | Дефект |
|---|---|
| `Employees.tsx` + `useDatabaseStore.setEmployees` | Импорт Excel не сохраняется в БД: `setEmployees` только меняет стейт, API не вызывается. Следующий `fetchAll()` затрёт импортированное |
| `Recruiting.tsx:275,284` | `\${...}` вместо `${...}` в template literal — классы drop-зоны и перетаскиваемой карточки не применяются |
| `Employees.tsx:82` | Колонка «Таб. номер» = `Math.random()`, новое значение на каждом рендере |
| `OrgChart.tsx:103` | Условие фильтра пропускает почти все узлы — дерево не строится |
| `tauriDb.ts` / `server.ts` | `resetSystem` удаляет 2 таблицы из 7 |
| `Employees.tsx` | Три селекта-фильтра и «Сбросить» ни к чему не подключены |
| `Employees.tsx` | Кнопка импорта скрыта классом `hidden` |
| `Layout.tsx:109,113` | `useAppStore.getState()` в рендере — компонент не подписан на изменения |
| `index.css:1` | Внешний `@import` шрифта — нарушение офлайн-требования |
| `tsconfig.json` | Нет `strict` |
| Проект | `@google/genai` и `motion` установлены и не используются; `README.md` и `.env.example` — артефакты AI Studio про Gemini |
| `/movements`, `/calendar` | Маршруты существуют, но недостижимы: нет пунктов в сайдбаре |

---

## Сводка

| Статус | Пунктов | Доля |
|---|---|---|
| **A** соответствует | 26 | ~17% |
| **B** в проекте лучше | 11 | ~7% |
| **C** реализовано иначе | 4 | ~3% |
| **D** частично | 44 | ~28% |
| **E** отсутствует | 54 | ~35% |
| **F** устарело | 7 | ~5% |
| **G** конфликтует | 9 | ~6% |

**Итого ~155 существенных пунктов.** Учтён пересмотр от 2026-08-23: «интеграционные тесты бэкенда» E→A, «e2e (Playwright)» F→A, «модульные тесты» E→D.

### Что делать с ТЗ

1. **Переписать под факт (B / C / G — 24 пункта):** React 19, Tailwind v4, отказ от shadcn/ui, двухрежимный фасад данных вместо «встроенного Express», TS-локали вместо JSON, расширенная система оформления, вопрос платформ.
2. **Удалить (F — 7 пунктов):** RTL, права доступа к шаблонам, нагрузочное тестирование, Tauri updater и автообновление, Figma-макеты, интервью Этапа 0. ~~Playwright~~ — исключён из этого списка 2026-08-23, требование восстановлено и выполнено.
3. **Оставить в плане работ (D + E — 99 пунктов):** это и есть реальный бэклог, он уже разложен по приоритетам в `BUILD_PROMPT.v2.md`.
4. **Дописать в ТЗ:** 8 модулей, построенных сверх задания.

---

## Решения заказчика (приняты 2026-08-01)

| Вопрос | Решение |
|---|---|
| Платформы | **Только Windows** |
| Валюта | **Настраиваемая** (символ, позиция, знаки, разделитель) |
| Вёрстка | **Desktop-only**, минимальное окно 1024×700 |
| Порог производительности | **10 000 сотрудников подтверждён** |
| 5 макетных модулей | **Довести до БД** |

### Как решения меняют статусы

| Пункт ТЗ | Было | Стало | Действие с ТЗ |
|---|---|---|---|
| 1.2а Платформы Win / macOS / Linux | G | **F** | Заменить на «Windows 10/11 x64» |
| Этап 1 · Tauri под три ОС | D | **A** | Переписать под Windows |
| Этап 8 · Тестирование на всех платформах | G | **F** | Удалить |
| Этап 9 · Инсталляторы `.dmg` / `.AppImage` / `.deb` | D | **F** | Удалить, оставить `nsis` + `msi` |
| НФТ · Кроссплатформенность | G | **F** | Заменить на «единая кодовая база, целевая ОС Windows» |
| Критерий приёмки · сборка десктопа | D | **A** | Выполнен |
| А.2 · Символ и позиция валюты | G | **E** | Конфликт снят, остаётся обязательной задачей |
| А.5 · Валюта и расчёт ФОТ | E | **E** | Подтверждено, приоритет повышен |
| 2.1м · Виртуализация таблиц | D | **E** | Обязательна. Порог 10 000 подтверждён |
| НФТ · 10 000 сотрудников ≤ 1 с | E | **E** | Подтверждено как приёмочное требование |
| 2.2б · SQLite — единственное хранилище | D | **D** | Подтверждено: 5 модулей переводятся на БД |
| 3.1–3.7 · пять макетных модулей | D | **D** | Подтверждены как обязательные, не демо |

Мобильной вёрстки ТЗ и не требовало — документ изначально описывает десктоп. Решение лишь фиксирует, что она не появится в скоупе, и снимает вопрос на будущее.

### Последствие, которое решения вскрыли

Подтверждение порога **10 000 сотрудников** делает блокирующей проблему, которая при малых объёмах не проявлялась: `useDatabaseStore` обёрнут в `persist` без `partialize` и сериализует все шесть массивов сущностей в localStorage при каждой мутации. При 10 000 сотрудников и годе табелей это гарантированный `QuotaExceededError` (квота ~5 МБ) плюс полная пересериализация стора на каждый клик по ячейке табеля.

`persist` из `useDatabaseStore` нужно убрать — источник истины и так SQLite, а localStorage-копия дополнительно маскирует дефект импорта Excel. Развёрнуто: `BUILD_PROMPT.v2.md`, раздел «Приоритет 0.5».

---

## Пересмотр от 2026-08-23 — тестовый контур

### Что изменилось в фактах

| Пункт ТЗ | Было | Стало | Основание |
|---|---|---|---|
| Этап 8 · Модульные тесты (Vitest) | E | **D** | Vitest установлен, 19 тестов. Расчётная логика ещё не покрыта |
| Этап 8 · Интеграционные тесты бэкенда | E | **A** | `tests/api/` — авторизация и CRUD по всем сущностям |
| Этап 8 · e2e (Playwright) | F | **A** | `tests/e2e/` — 23 теста, обход всех 18 экранов |
| НФТ · Качество | — | **A** | `npm run verify` = типы → сборка → API → e2e, одной командой |

### Почему вердикт «F» по Playwright отменён

Первоначальная оценка признала e2e несоразмерным однопользовательскому офлайн-приложению. Оценка была основана на предположении о стоимости, и предположение оказалось неверным.

Фактическая цена контура: три файла тестов, конфиг, вспомогательный модуль-наблюдатель — около 250 строк, один вечер работы, 1,8 минуты на прогон. Взамен получено то, что вручную не проверялось никогда: одновременный контроль всех 18 экранов на ошибки консоли, необработанные исключения и неуспешные обращения к API. Ручной обход того же объёма занимает у человека 15–20 минут и ловит только видимые глазом поломки.

Довод «приложение однопользовательское» относился к сложности сценариев — многопользовательские, конкурентные, ролевые проверки здесь действительно не нужны, и их нет. Но он не относился к самому факту автоматизации обхода. Эти две вещи в исходном аудите были смешаны — это и была ошибка вердикта.

Вердикт **F по нагрузочному тестированию сохраняется** и на него это рассуждение не переносится: нагрузочный стенд для одного пользователя на одном устройстве по-прежнему бессмыслен, а порог 10 000 сотрудников закрывается сид-базой и замером конкретных сценариев.

### Изменения в коде приложения, сделанные ради тестируемости

Три точечные правки, поведение приложения при обычном запуске не меняющие:

| Файл | Изменение | Зачем |
|---|---|---|
| `src/db/sqlite.ts` | Путь к БД читается из `DB_PATH`, по умолчанию — прежняя `local-hr-docs.db` | Изоляция: тесты работают на временной базе и не трогают рабочие данные |
| `server.ts` | Экспорт `app` и `startServer`; автозапуск отключается через `HR_NO_AUTOSTART=1` | API-тесты поднимают Express в своём процессе без vite — прогон 3 секунды вместо десятков |
| `playwright.config.ts` | Для e2e выставляется `DISABLE_HMR=true` (флаг уже был в `vite.config.ts`) | HMR-вебсокет vite сыплет ошибками в консоль и мешает дождаться `networkidle` |

### Что тестовый контур НЕ закрывает

Это ограничения, а не недоделки — фиксируются, чтобы зелёный прогон не читали шире, чем он есть:

1. **Нативный режим Tauri.** Тесты гоняют веб-ветку через Express. В десктопной сборке данные идут напрямую в SQLite через `src/data/tauriDb.ts` — второй независимый путь, не покрытый ничем. Проверяется вручную: `npm run tauri:dev`.
2. **Корректность расчётов.** Проверяется, что экраны открываются и API отвечает, а не что итоги табеля, ФОТ и подстановка переменных считаются верно. Это задача 4.4.8.
3. **Визуальная корректность.** Ошибок консоли нет — но съехавшая вёрстка, неправильный цвет или нечитаемый текст тестами не ловятся. Скриншотных сравнений нет.
4. **Три локализации.** Обход идёт на языке по умолчанию. Паритет ключей ru/en/tk автоматом пока не проверяется.
