# Техническое задание

## Информационная система управления кадрами и справочным документооборотом
### HRDesk

**Версия документа:** 1.1
**Дата:** 2026-08-01
**Тип разработки:** десктоп-приложение под Windows, 100% локальное и однопользовательское (один пользователь — одно устройство), без сети и синхронизации

> **О версии 1.1.** Документ приведён в соответствие с фактически построенной системой. Исправлены пункты, противоречившие реализации; удалены неактуальные; в состав системы внесены модули, построенные сверх первоначального задания. Требования, которые остаются невыполненными, сохранены без изменений — это действующий объём работ.
>
> **Сопутствующие документы:**
> - `BUILD_PROMPT.v2.md` — рабочий план: приоритеты, конкретные задачи, критерии готовности. Живой документ.
> - `TZ_AUDIT.md` — постатейная сверка версии 1.0 с кодом на 2026-08-01. Основание для правок этой версии. Датированный снимок, не поддерживается.

---

## 1. Общие сведения

### 1.1. Назначение системы

Система предназначена для автоматизации кадрового учёта, управления штатным расписанием и формирования справочных документов на предприятии. Ключевое требование — современный, интуитивный интерфейс, принципиально отличающийся от интерфейса «1С:Предприятие» по эргономике, скорости работы и визуальному стилю, при сохранении возможности обмена данными с системами «1С» через файлы.

### 1.2. Ключевые характеристики

| Характеристика | Значение |
|---|---|
| Платформа | **Windows 10/11 x64** (десктоп) |
| Форм-фактор | **Только десктоп.** Опорное разрешение 1280×800, минимальное окно 1024×700. Мобильная и планшетная вёрстка не разрабатывается |
| Режим работы | 100% локальный, однопользовательский, полностью офлайн (без сети и синхронизации), включая шрифты и любые ресурсы |
| Языки интерфейса | Туркменский, английский, русский |
| Язык по умолчанию | Выбирается пользователем, сохраняется локально |
| Валюта | **Настраиваемая:** символ, позиция (префикс/суффикс), число знаков после запятой, разделитель разрядов |
| Форматы выгрузки отчётов | Excel (.xlsx), JSON |
| Форматы обмена | Файловый импорт/экспорт: Excel (.xlsx), XML, CSV, Word (.docx) |
| Расчётный объём данных | До 10 000 сотрудников с сохранением отзывчивости интерфейса |
| Основной стек | React, TypeScript, Vite, Tailwind CSS, Tauri, SQLite |

### 1.3. Термины и сокращения

- **ШР** — штатное расписание.
- **Конструктор справок** — визуальный редактор шаблонов справочных документов.
- **Локальное приложение** — приложение, полностью работающее на одном устройстве без сети, внешнего сервера и синхронизации; все данные хранятся в локальной БД.
- **i18n** — интернационализация (поддержка нескольких языков).
- **Дизайн-токены** — семантические CSS-переменные (уровни фона, границ, текста, акцента), через которые задаётся всё оформление; обеспечивают согласованность и переключение тем.

### 1.4. Зафиксированные решения заказчика

Приняты 2026-08-01, имеют приоритет над любыми положениями версии 1.0.

| № | Решение |
|---|---|
| Р-1 | Целевая платформа — **только Windows**. Сборки под macOS и Linux не разрабатываются |
| Р-2 | Валюта — **настраиваемая** в модуле настроек, единая для всей системы |
| Р-3 | Форм-фактор — **только десктоп**. Мобильная и планшетная вёрстка в объём работ не входит |
| Р-4 | Требование производительности **до 10 000 сотрудников подтверждено** и входит в приёмку |
| Р-5 | Модули «Рекрутинг», «Отсутствия», «Адаптация», «Оценка», «База знаний» — **полноценные модули с хранением в БД**, а не демонстрационные экраны |
| Р-6 | **Автоматическое тестирование входит в объём работ** (принято 2026-08-23). Обязательны автотесты локального API и сквозной автоматический обход всех экранов. Отменяет вывод `TZ_AUDIT.md` о том, что e2e-тестирование несоразмерно однопользовательскому приложению — см. п. 2.6 |

---

## 2. Архитектура и стек технологий

Раздел описывает фактически реализованную архитектуру. Она принята как целевая; отклонения от неё требуют отдельного согласования.

### 2.1. Клиент

| Слой | Технология | Примечание |
|---|---|---|
| UI-фреймворк | **React 19** + TypeScript 5.8 | |
| Сборка | Vite 6 | |
| Стили | **Tailwind CSS v4**, конфигурация CSS-first через директиву `@theme` | Отдельный `tailwind.config.js` не используется |
| Библиотека компонентов | **Собственные компоненты на дизайн-токенах** | Готовые библиотеки (shadcn/ui, Radix, MUI) не применяются: весь интерфейс построен на единой системе токенов, внедрение сторонней библиотеки потребовало бы переписывания всех экранов без выигрыша |
| Десктоп-оболочка | **Tauri 2** (Rust) | Лёгкая нативная оболочка, современный вид, принципиально отличный от «1С» |
| Клиентское состояние | Zustand | Два стора: настройки и сессия (с сохранением в localStorage) и кэш доменных данных (в памяти, источник истины — SQLite) |
| Формы и валидация | React Hook Form + Zod | Обязательны для всех форм ввода доменных данных |
| Локальная БД | SQLite | Единственное хранилище данных на устройстве |
| Интернационализация | i18next + react-i18next, словари как **модули TypeScript** | Типобезопасно, гарантированно доступно офлайн, паритет ключей проверяется автоматически |
| Графики | Recharts | Цвета осей, сетки и подсказок берутся из дизайн-токенов |
| Таблицы | TanStack Table + **виртуализация списков** | Виртуализация обязательна для соответствия требованию по 10 000 записей |
| Rich-text | TipTap | Редактор статей базы знаний |
| Drag-and-drop | `@hello-pangea/dnd` | Канбан-доски и перестановка блоков шаблона |

### 2.2. Доступ к данным

Приложение не содержит сетевого сервера. Доступ к данным организован через **единый фасад с двумя реализациями**, выбираемыми по среде выполнения:

| Среда | Реализация |
|---|---|
| **Продуктивная сборка (Tauri)** | Прямые SQL-запросы к локальной SQLite через плагин Tauri SQL. Промежуточный сервер отсутствует |
| **Режим разработки (браузер)** | Локальный Express поверх той же схемы SQLite, отдающий REST на `/api/*`, совмещённый с dev-сервером Vite |

Обе реализации предоставляют интерфейсу одинаковый набор операций. Прикладной код не знает, в какой среде исполняется.

**Требования к слою данных:**
- Схема БД и начальные данные описываются **в одном месте** и используются обеими реализациями. Дублирование схемы недопустимо.
- Изменения схемы оформляются версионированными миграциями.
- Таблицы снабжаются индексами по полям, участвующим в фильтрации и связях.
- Доменные данные **не сохраняются в localStorage**: источник истины — SQLite, оперативный кэш — память приложения. В localStorage допустимо хранить только настройки интерфейса и признак сессии.
- Данные, растущие во времени (табели, архив), загружаются срезами по выбранному периоду, а не целиком.

### 2.3. Аутентификация

Единственная локальная учётная запись владельца устройства. Пароль хранится в виде хеша bcrypt. Ролевая модель отсутствует.

### 2.4. Библиотеки экспорта и обмена

| Задача | Технология |
|---|---|
| Генерация Excel | ExcelJS (форматирование, стили, автоширина, итоги) |
| Чтение Excel при импорте | SheetJS |
| Генерация Word | docxtemplater |
| Генерация PDF | Печать средствами оболочки; html2pdf для табличных выгрузок |
| Экспорт JSON | Нативный, по документированной схеме |
| Обмен с внешними системами | Только файловый (XML, CSV, Excel) |

> **Примечание.** Любой обмен данными выполняется через локальные файлы. Живые сетевые интеграции (в т. ч. с 1С по OData/REST), внешний сервер, синхронизация и автообновление приложения в проекте не предусмотрены — они противоречат требованию полной автономности.

### 2.5. Дизайн-система

Оформление задаётся семантическими токенами, а не прямыми цветами. Обязательный состав:

- **Уровни фона:** приложение → секция → карточка → вложенная карточка → контрол → усиленный контрол → поле ввода → состояние наведения.
- **Границы:** слабая / базовая / усиленная.
- **Текст:** основной / вторичный / приглушённый / едва заметный.
- **Акцент:** палитра из 10 оттенков, переключаемая пользователем.

Каждый токен определён для светлой и тёмной тем. Прямое указание цвета в коде интерфейса не допускается; исключения — печатный документ (всегда белая «бумага»), категориальная палитра графиков и цвета, применяемые пользователем к содержимому статей.

### 2.6. Автоматическое тестирование

Реализовано 2026-08-23 согласно решению Р-6.

| Уровень | Технология | Объём |
|---|---|---|
| Локальное API | Vitest | Авторизация, смена пароля, полные циклы создания / чтения / изменения / удаления по всем сущностям: сотрудники, подразделения, должности, шаблоны, табель, архив |
| Сквозной обход интерфейса | Playwright | Вход в систему, открытие каждого экрана системы, переход по всем пунктам навигации, поведение при отсутствии сессии и при неверном пароле |
| Расчётная логика | Vitest | Планируется: итоги табеля, ФОТ, занятость штатных единиц, подстановка переменных в документах, обмен через Excel, паритет ключей локализации |

Требования к тестовому контуру:

- **Одна команда для полной проверки.** Типы, сборка, тесты API и сквозной обход запускаются единой командой и дают однозначный результат «годен / не годен».
- **Изоляция данных.** Тесты работают с отдельной временной базой, создаваемой на время прогона. Рабочая база пользователя не читается и не изменяется ни при каких обстоятельствах.
- **Автономность.** Прогон не требует сети и внешних сервисов. Браузер для сквозного обхода устанавливается один раз локально.
- **Обнаружение скрытых отказов.** Сквозной обход фиксирует не только видимые поломки вёрстки, но и ошибки в консоли браузера, необработанные исключения и неуспешные обращения к локальному API — экран считается исправным только при их полном отсутствии.
- **Синхронность с кодом.** Появление нового экрана обязывает включить его в список автоматического обхода, появление нового метода API — покрыть его тестом.

> **Граница покрытия.** Автотесты проверяют веб-режим доступа к данным. Нативный режим (прямая работа с SQLite в десктопной сборке) — второй независимый путь к данным, автотестами не покрыт и проверяется вручную. Это ограничение осознанное и подлежит устранению по мере развития системы.

---

## 3. Состав системы

### 3.1. Модули первоначального задания

1. **Кадровый учёт** — карточки сотрудников, приём/перевод/изменение оклада/увольнение, история изменений.
2. **Штатное расписание** — дерево подразделений, должности, ставки, штатные единицы, вакансии, версии ШР.
3. **Конструктор справок** — визуальное создание шаблонов, поля-подстановки, генерация документов.
4. **Отчётность** — предустановленные отчёты, конструктор отчётов, выгрузка в Excel и JSON.
5. **Файловый обмен** — импорт/экспорт Excel/XML/CSV, генерация Word.
6. **Администрирование** — язык, локальный пароль владельца, журнал аудита.
7. **Настройки (централизованные)** — единый центр всех параметров системы (Приложение А).

### 3.2. Модули, построенные сверх первоначального задания

Введены в ходе разработки, входят в состав системы и в приёмку (решение Р-5).

8. **Табель учёта рабочего времени** — форма Т-13: буквенные коды явок, навигация по периодам, автоматические выходные, итоги по дням, явкам и часам, фильтр по подразделению.
9. **Рекрутинг** — канбан-доска кандидатов с перетаскиванием между стадиями подбора, поиск, фильтры.
10. **Отсутствия** — заявки на отпуск, больничный, отгул, удалённую работу; согласование и отклонение.
11. **Адаптация и увольнение** — чек-листы задач onboarding и offboarding с ответственными и прогрессом.
12. **Оценка персонала** — цели с прогрессом, оценочные кампании, ревью 360°.
13. **База знаний** — статьи с форматированным текстом, категории, полнотекстовый поиск.
14. **Архив** — данные завершённых периодов, фильтры по году, подразделению и сотруднику, выгрузка в Excel и PDF, печать.
15. **Профиль пользователя** — данные владельца, настройки языка и темы, безопасность.

### 3.3. Общие требования к модулям

- Данные любого модуля хранятся в локальной SQLite. Хранение доменных данных только в состоянии интерфейса не допускается.
- Каждый экран имеет состояния: загрузка, пусто, ошибка, успех.
- Все строки интерфейса выведены в словари локализации; в коде экранов текст не хардкодится.
- Каждый модуль регистрирует свои параметры в централизованном модуле настроек.
- Каждый рабочий модуль доступен из главного меню; пункт меню не ведёт на незавершённый экран.

---

## 4. Организация работ

Первоначальный поэтапный план (версия 1.0, этапы 0–9) отражал разработку с нуля и более не применим: система построена частично, последовательность работ определяется фактическим состоянием кода.

**Актуальный план работ, приоритеты и постановки задач — в `BUILD_PROMPT.v2.md`.** Он ведётся как живой документ и имеет приоритет при планировании.

### 4.1. Текущее состояние по модулям

| Модуль | Состояние |
|---|---|
| Локальный вход, смена пароля | Выполнено |
| Трёхъязычный интерфейс, выбор языка по умолчанию | Выполнено; требуется вычитка туркменского носителем |
| Дизайн-система, светлая и тёмная темы, настройки оформления | Выполнено, превышает первоначальные требования |
| Кадровый учёт — справочник, карточка, поиск | Выполнено частично: нет контактов, документов, фото; нет кадровых операций и истории |
| Штатное расписание | Выполнено частично: нет дерева подразделений, версий ШР, справочника надбавок |
| Конструктор справок | Выполнено частично: нет перестановки блоков, условной логики, версий шаблонов |
| Генерация документов | Выполнено для печати и PDF; выгрузка `.docx` не подключена |
| Табель Т-13 | Выполнено |
| Архив | Выполнено |
| Отчётность | Выполнено частично: часть отчётов на демонстрационных данных, конструктор отчётов отсутствует, выгрузка не подключена |
| Рекрутинг, Отсутствия, Адаптация, Оценка, База знаний | Интерфейс готов, хранение в БД отсутствует |
| Движение кадров, Календарь | Не реализованы |
| Файловый обмен | Выполнен экспорт в Excel; импорт не сохраняет данные; XML, CSV, JSON отсутствуют |
| Настройки | Выполнены оформление, смена пароля, сброс; остальные разделы отсутствуют |
| Резервное копирование, журнал аудита, шифрование, блокировка по бездействию | Не реализованы |
| Виртуализация и готовность к 10 000 записей | Не реализованы |
| Автотесты локального API и сквозной обход экранов | Выполнено 2026-08-23: 19 тестов API, 23 сквозных теста, полная проверка одной командой |
| Модульные тесты расчётной логики | Не реализованы |
| Автотесты нативного режима доступа к данным | Не реализованы |
| Линтер, строгий режим TypeScript | Не настроены |

### 4.2. Порядок работ

1. Устранение дефектов в существующем коде.
2. Приведение архитектуры хранения к требованию по 10 000 записей (снятие сохранения доменных данных в localStorage, срезовая загрузка, виртуализация, индексы).
3. Перевод пяти модулей с интерфейсным состоянием на хранение в БД.
4. Реализация отсутствующих модулей.
5. Данные, надёжность, наполнение настроек.
6. Качество кода, тесты, документация.

---

## 5. Нефункциональные требования

| Категория | Требование |
|---|---|
| Производительность | Открытие основных экранов ≤ 1 с. Список из **10 000 сотрудников** — плавная прокрутка за счёт виртуализации. Поиск отзывчив при том же объёме |
| Объём данных | Хранение табелей и архива не менее чем за 5 лет при штате 10 000 человек без деградации интерфейса |
| Локальная работа | 100% работоспособность без сети. Все ресурсы, включая шрифты, поставляются в составе приложения |
| Безопасность | Шифрование локальной БД, защита персональных данных на устройстве, блокировка по бездействию, журнал аудита действий |
| Платформа | Windows 10/11 x64, единая кодовая база |
| Форм-фактор | Десктоп. Корректная работа при минимальном окне 1024×700 |
| Локализация | 3 языка (tk/en/ru), выбор языка по умолчанию пользователем, форматирование дат, чисел и валюты по выбранной локали |
| Масштабируемость | Модульная архитектура, возможность добавления новых модулей и отчётов |
| UX | Современный интерфейс, отличный от «1С»: чистая типографика, светлая и тёмная темы, настраиваемые плотность и размер шрифта, минимум кликов |
| Совместимость | Файловый обмен (Excel, XML/CSV, Word) без потери данных |
| Проверяемость | Полная проверка системы (типы, сборка, тесты API, сквозной обход всех экранов) выполняется одной командой, без сети и без доступа к рабочим данным пользователя. Появление нового экрана или метода API обязывает расширить автотесты — см. п. 2.6 |
| Качество кода | Строгий режим TypeScript, статический анализ без ошибок, покрытие расчётной логики модульными тестами |

---

## 6. Пользователь системы

Приложение однопользовательское: единственная учётная запись — **владелец устройства** с полным доступом ко всем функциям. Ролевая модель (RBAC), управление пользователями и разграничение прав не применяются. Это относится в том числе к шаблонам справок, отчётам и настройкам — областей действия и уровней доступа в системе нет.

---

## 7. Критерии приёмки

Отметка «✓» — требование выполнено на дату версии документа.

### 7.1. Платформа и автономность

- ✓ Приложение собирается и запускается как десктоп-приложение Windows (`nsis`, `msi`) из единой кодовой базы.
- Приложение работает 100% локально: без сети, внешнего сервера, синхронизации и внешних ресурсов, включая шрифты.
- ✓ Все данные хранятся в локальной SQLite; доменные данные не дублируются в localStorage.
- Корректная работа при минимальном окне 1024×700; горизонтальные таблицы и модальные окна не ломают раскладку.

### 7.2. Локализация

- ✓ Интерфейс доступен на туркменском, английском и русском; любой язык назначается языком по умолчанию, выбор сохраняется между запусками.
- Непереведённых строк в интерфейсе нет, включая демонстрационные и служебные данные.
- Даты, числа и валюта форматируются в соответствии с выбранной локалью и настройками.
- Туркменская локализация вычитана носителем языка.

### 7.3. Функциональность

- Кадровый учёт: карточка с контактами и документами, кадровые операции (приём, перевод, изменение оклада, увольнение), история изменений по каждому сотруднику.
- Штатное расписание: дерево подразделений, штатные единицы, вакансии, версионирование ШР со сравнением редакций, сводка по ФОТ.
- ✓ Конструктор справок позволяет создать шаблон и сформировать справку с подстановкой данных сотрудника.
- Конструктор справок поддерживает перестановку блоков, условную логику и версионирование шаблонов.
- Документы выгружаются в PDF и в Word (.docx) на фирменном бланке.
- Отчёты выгружаются в Excel (.xlsx) и JSON; работает конструктор отчётов с сохраняемыми пресетами.
- Реализован файловый импорт (Excel/XML/CSV) с настраиваемым маппингом, валидацией и предпросмотром; импортированные данные сохраняются в БД.
- ✓ Табель учёта рабочего времени ведётся и сохраняется, итоги считаются корректно.
- Модули «Рекрутинг», «Отсутствия», «Адаптация», «Оценка», «База знаний» хранят данные в БД.
- Каждый рабочий модуль доступен из главного меню; ни один пункт меню не ведёт на заглушку.

### 7.4. Настройки и данные

- Все настраиваемые функции доступны из единого модуля настроек, хранятся локально и корректно применяются.
- ✓ Работают настройки оформления: тема, акцентный цвет, плотность, размер шрифта.
- Валюта настраивается и единообразно применяется на всех экранах, в документах и во всех выгрузках.
- Работают резервное копирование и восстановление из копии.
- Реализованы все четыре уровня сброса данных; полный сброс действительно удаляет все локальные данные.
- Ведётся журнал аудита действий с фильтрацией и выгрузкой.

### 7.5. Безопасность

- Локальная БД зашифрована.
- Работает блокировка приложения по бездействию.
- Пароль владельца задаётся при первом запуске; учётные данные не отображаются в интерфейсе.

### 7.6. Качество и производительность

- На базе из 10 000 сотрудников: холодный старт и открытие основных экранов ≤ 1 с, прокрутка списков и табеля без рывков.
- Строгий режим TypeScript и статический анализ проходят без ошибок; сборка успешна.
- ✓ Локальное API покрыто автотестами: авторизация, смена пароля и полные циклы работы с данными по всем сущностям.
- ✓ Каждый экран системы проходит автоматический сквозной обход: открывается, отрисовывается, не даёт ошибок в консоли браузера и неуспешных обращений к API; все пункты навигации ведут на рабочие экраны.
- ✓ Полная проверка (типы, сборка, тесты API, сквозной обход) выполняется одной командой и не затрагивает рабочую базу данных.
- Расчётная логика (итоги табеля, ФОТ, занятость штатных единиц, подстановка переменных, импорт и экспорт) покрыта модульными тестами.
- ✓ Интерфейс визуально и по эргономике отличается от «1С:Предприятие».
- Подготовлено руководство пользователя на трёх языках.

---

## Приложение А. Модуль настроек — перечень настраиваемых функций

Модуль настроек — единая точка управления всеми параметрами системы. Приложение однопользовательское и полностью локальное, поэтому все параметры хранятся на устройстве и применяются к единственному пользователю — без областей действия и без разграничения по ролям.

> Постатейное состояние выполнения — в `TZ_AUDIT.md`. На дату версии документа реализованы разделы А.1 (частично), А.2 (язык), А.3 (смена пароля) и А.10.1 (частично).

### А.1. Интерфейс и внешний вид
- Тема оформления: светлая / тёмная / системная
- Акцентный цвет: выбор из палитры
- Плотность интерфейса: компактная / стандартная / просторная
- Размер шрифта: три ступени
- Параметры доступности: повышенный контраст, снижение анимаций
- Стартовый экран и состав бокового меню (закрепление модулей)
- Формат отображения списков (таблица / карточки), число строк на странице

### А.2. Язык и локализация
- **Язык по умолчанию: туркменский / английский / русский**
- Формат даты и времени, часовой пояс
- Формат чисел и разделитель разрядов
- **Валюта: символ, позиция (префикс / суффикс), число знаков после запятой.** Настройка единая для всей системы и применяется на всех экранах, в документах и во всех выгрузках
- Первый день недели, рабочий календарь и праздники

### А.3. Профиль и аккаунт
- ФИО, фото, контактные данные, подпись/факсимиле
- Смена пароля, политика паролей (длина, сложность, срок действия)
- Блокировка приложения по бездействию

### А.4. Кадровый модуль
- Пользовательские поля карточки сотрудника (добавление/скрытие полей)
- Справочники: типы документов, причины приёма/увольнения, категории
- Обязательность полей и правила валидации

### А.5. Штатное расписание
- Параметры расчёта ФОТ и округления (валюта задаётся в А.2)
- Справочники должностей, тарифных сеток, надбавок
- Порядок утверждения версий ШР
- Пороги индикаторов вакансий и переработок

### А.6. Конструктор справок
- Фирменный бланк: логотип, шапка, колонтитулы, реквизиты организации
- Шрифты, стили и поля документа по умолчанию
- Управление библиотекой шаблонов
- Правила нумерации и регистрации выданных справок
- Блоки подписей и печатей, факсимиле ответственных лиц

### А.7. Отчёты и экспорт
- Формат экспорта по умолчанию: **Excel (.xlsx) / JSON**
- Стили Excel по умолчанию (шапка, итоги, автоширина, логотип)
- Схема и структура JSON-выгрузки
- Сохранённые пресеты отчётов (фильтры, поля, группировки)

### А.8. Файловый обмен (Excel, XML/CSV, Word)
- **Импорт:** правила импорта Excel/XML/CSV (сопоставление колонок, кодировка, разделители) и маппинг полей
- **Экспорт:** каталоги по умолчанию и форматы файлов для выгрузки
- **Word:** шаблоны .docx, бланк и поля-подстановки по умолчанию

### А.9. Уведомления
- Внутренние уведомления приложения (вкл./выкл.). Без e-mail/push и любых внешних каналов
- События для оповещения (готовность отчёта, кадровые события)
- Режим «Не беспокоить» и расписание уведомлений

### А.10. Данные и резервное копирование
- Расписание и глубина локального резервного копирования БД
- Восстановление из резервной копии
- Экспорт/импорт настроек системы (перенос конфигурации)
- Папка хранения локальных данных на устройстве

### А.10.1. Сброс данных

Функция сброса реализуется с несколькими уровнями, чтобы разделить безопасные операции и необратимые. Каждая операция требует подтверждения; необратимые операции требуют повторного ввода пароля, фиксируются в журнале аудита и по умолчанию сопровождаются предложением создать резервную копию.

- **Сброс пользовательских настроек к значениям по умолчанию** — возвращает тему, язык, пресеты, раскладку интерфейса к исходным; данные не затрагиваются.
- **Сброс настроек модуля/системы к заводским** — откат конфигурации без удаления кадровых данных.
- **Очистка данных отдельного модуля** — удаление данных выбранных модулей (кандидаты, заявки, чек-листы, статьи, табели, архив) с явным выбором области.
- **Полный сброс системы (factory reset)** — необратимое удаление **всех** локальных данных и настроек: должны очищаться все таблицы без исключения. Двойное подтверждение (ввод пароля + фраза-подтверждение), обязательная резервная копия перед выполнением, запись в аудит.

> Частичное удаление под видом полного сброса недопустимо. Операции, поддерживающие откат, восстанавливаются из резервной копии.

### А.11. Аудит, безопасность и система
- Уровень детализации журнала аудита и срок хранения записей
- Шифрование локальной базы (вкл./выкл.), политика блокировки при бездействии
- Сведения о системе, версия, лицензия — просмотр

---

*Документ является основой для детализации в частных технических заданиях по каждому модулю. Оперативное планирование ведётся в `BUILD_PROMPT.v2.md`.*
